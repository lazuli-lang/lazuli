{
  "name": "@lazuli/vite",
  "version": "0.1.0",
  "description": "Vite alias resolver for Lazuli-driven apps — reads Lazurite.toml at vite-config load time and returns the @lazuli/runtime/* + @lazuli/plugin-* alias array, eliminating absolute filesystem paths from vite.config.ts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "smol-toml": "^1.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
