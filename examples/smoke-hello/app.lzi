app SmokeHello
  uses
    hello

  targets
    backend go

  environments
    local

  urls
    api local "http://localhost:8080"

  runtime
    unit api
      serves commands, apis
      healthcheck "/healthz"

  deploy
    migrations disabled
    destructive_migrations forbidden
    rollback disabled
