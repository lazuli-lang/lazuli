app MarketplaceShapedAuth
  uses org
  uses account

  targets
    backend go

  environments
    dev

  urls
    api dev "http://localhost:8085"

  runtime
    unit api
      serves commands, apis
      healthcheck "/healthz"

  deploy
    migrations disabled
    destructive_migrations forbidden
    rollback disabled
