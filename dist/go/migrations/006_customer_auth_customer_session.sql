-- Code generated by lazuli; DO NOT EDIT.
-- source: AcmeCRM
-- resource: customer_auth.CustomerSession

CREATE TABLE IF NOT EXISTS "customer_session" (
    id BIGSERIAL PRIMARY KEY,
    org_id BIGINT NOT NULL,
    customer BIGINT,
    refresh_token_hash TEXT NOT NULL,
    provider_access_token BYTEA, -- lazuli:encrypted @key.tenant algorithm=aes_256_gcm
    provider TEXT NOT NULL,
    expires_at TIMESTAMPTZ NOT NULL,
    refresh_expires_at TIMESTAMPTZ,
    parent_session_id BIGINT,
    theft_detected_at TIMESTAMPTZ,
    created_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    FOREIGN KEY (customer) REFERENCES "customer" (id)
);

CREATE INDEX ON "customer_session" (parent_session_id) WHERE parent_session_id IS NOT NULL;

CREATE INDEX ON "customer_session" (refresh_token_hash) WHERE refresh_token_hash != '';
