// Code generated by lazuli; DO NOT EDIT.
import * as React from "react";
import { Link, createRootRouteWithContext, createRoute, createRouter, type LinkProps } from "@tanstack/react-router";
import type { QueryClient } from "@tanstack/react-query";
import type { LazuliClient } from "@lazuli/runtime";

export const routeSpecs = [
  {
    name: "host_booking_detail",
    path: "/host/bookings/$booking_id",
    audience: "host",
    component: "hostBookingDetail",
  },
  {
    name: "host_home",
    path: "/host",
    audience: "host",
    component: "hostHome",
  },
] as const;

export type ClientComponentKey = "hostBookingDetail" | "hostHome";
export type GeneratedRouterContext = { client: LazuliClient; queryClient: QueryClient };
export type GeneratedRouterComponents = Record<ClientComponentKey, React.ComponentType>;

export function createGeneratedRouter(options: {
  client: LazuliClient;
  queryClient: QueryClient;
  components: GeneratedRouterComponents;
  notFoundComponent?: React.ComponentType;
}) {
  const rootRoute = createRootRouteWithContext<GeneratedRouterContext>()({
    component: options.notFoundComponent ?? (() => null),
  });
  const hostBookingDetailRoute = createRoute({
    getParentRoute: () => rootRoute,
    path: "/host/bookings/$booking_id",
    component: options.components.hostBookingDetail,
  });
  const hostHomeRoute = createRoute({
    getParentRoute: () => rootRoute,
    path: "/host",
    component: options.components.hostHome,
  });
  const routeTree = rootRoute.addChildren([hostBookingDetailRoute, hostHomeRoute]);
  return createRouter({
    routeTree,
    context: { client: options.client, queryClient: options.queryClient },
  });
}

export const AppLink = Link as <T extends LinkProps["to"]>(
  props: LinkProps & { to: T },
) => React.ReactElement;
