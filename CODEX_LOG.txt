Reading prompt from stdin...
OpenAI Codex v0.130.0
--------
workdir: c:\tmp\migrate-rhf-adapter
model: gpt-5.5
provider: openai
approval: never
sandbox: danger-full-access
reasoning effort: xhigh
reasoning summaries: none
session id: 019e5273-d38d-78c1-abe0-21d83857431f
--------
user
# useLazuliForm + RHF adapter (`@lazuli/runtime/react/rhf` subpath)

## What

Build the M.2 prereq: a React Hook Form adapter that bridges the headless `useLazuliForm` (Wave A.3) to apps that use RHF's `<Controller>` pattern with UI primitives (`<Input>`, `<Select>`, `<DatePicker>` consuming `field.value` / `field.onChange` / `fieldState.error`).

This unblocks the hostpoint settings panel migration (14 panels can collapse from ~150 LoC to ~50 LoC each).

## Where

- Repo: `c:/Users/lucas/lazuli` (worktree `c:/tmp/migrate-rhf-adapter`)
- New file: `runtime/ts/lazuli/src/react-rhf.ts` (subpath entry)
- Update `runtime/ts/lazuli/package.json` exports to add `./react/rhf` subpath
- Spec: `c:/Users/lucas/lazuli-ops/docs/proposals/hostpoint-migration-backlog-2026-05-22.md` §M.2 unblock spec

## Why

`useLazuliForm` exposes `{values, setValue, setValues, reset, handleSubmit, isDirty, isSubmitting, error, serverErrors}` — but RHF apps want `register` + `Controller` integration. The adapter exposes both surfaces.

## How

Signature:

```ts
import { useForm, type UseFormProps } from "react-hook-form";
import { zodResolver } from "@hookform/resolvers/zod";
import type { ZodSchema } from "zod";
import type { CommandSpec, QuerySpec } from "@lazuli/runtime";

export type UseLazuliFormRHFOptions<Values, SubmitInput, SubmitOutput> = {
  defaults: Values;
  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
  submit: CommandSpec<SubmitInput, SubmitOutput>;
  mapValuesToInput: (values: Values) => SubmitInput;
  schema?: ZodSchema<Values>;
  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
  onSuccess?: (out: SubmitOutput, values: Values) => void;
  onError?: (err: unknown, values: Values) => void;
};

export function useLazuliFormRHF<Values, Input, Output>(
  opts: UseLazuliFormRHFOptions<Values, Input, Output>
): {
  /** Pass to <Controller>, useFieldArray, watch, register, etc. */
  control: ReturnType<typeof useForm<Values>>["control"];
  formState: ReturnType<typeof useForm<Values>>["formState"];
  /** Wired submit handler (calls submit command + onSuccess/onError). */
  onSubmit: () => Promise<void>;
  isSubmitting: boolean;
  /** Raw error from the last submit (LazuliError or unknown). */
  submitError: unknown;
  /** Reset to defaults (or to a partial override). */
  reset: (next?: Partial<Values>) => void;
};
```

Implementation:

1. Construct `useForm` from RHF with `defaultValues: opts.defaults` and `resolver: opts.schema ? zodResolver(opts.schema) : undefined`.
2. Construct `useLazuliQuery(opts.hydrateFrom, {})` when present. On data, call `reset(mergedValues)` once (use a ref to prevent re-hydration after user typing).
3. Construct `useLazuliCommand(opts.submit)` for the mutation.
4. `onSubmit` = `handleSubmit(async (values) => { try { await mutateAsync(mapValuesToInput(values)); opts.onSuccess?.(out, values); } catch (err) { /* server validation_failed → setError per field; otherwise opts.onError */ } })`.
5. Server validation: if `LazuliError.code === 'validation_failed'` and `error.data?.fields` is `{<fieldName>: <message>}`, call `setError(fieldName, { type: 'server', message })` for each.

## Test plan

Tests in `runtime/ts/lazuli/src/react-rhf.test.tsx`:
- defaults populate `useForm` correctly
- hydrateFrom query → `reset(data)` fires once
- subsequent query refetches don't overwrite user-typed values
- onSubmit calls command with mapped input
- onSuccess fires post-mutation
- validation_failed with field errors → `formState.errors[field]` populated
- reset(partial) overrides
- typecheck via test fixture using generic shape

## Constraints

- **RHF + zod-resolver are peer deps** — add to `runtime/ts/lazuli/package.json` peerDependencies. Apps already have them; runtime doesn't bundle.
- **Subpath isolated** — `@lazuli/runtime/react/rhf` is the import. Don't pollute `@lazuli/runtime/react` core.
- **No new top-level namespace.**

## Acceptance

- `runtime/ts/lazuli/src/react-rhf.ts` created.
- `package.json` exports map includes `./react/rhf`.
- `pnpm test` adds N new test cases (suggest ≥6).
- Sample callsite in STATUS.md showing hostpoint's `HostPersonal.tsx` (149 LoC) collapsed to ~50 LoC using the adapter.
- `STATUS.md` at worktree root.

## Commit message template

```
runtime: @lazuli/runtime/react/rhf — useLazuliFormRHF adapter

Bridges Wave A.3's headless useLazuliForm to apps using react-hook-form
with <Controller> bindings to UI primitives. Unblocks M.2 settings
panel migration (each panel: useForm + zodResolver + manual hydrate +
manual save + manual error → useLazuliFormRHF in ~3 lines).

- New subpath: @lazuli/runtime/react/rhf
- RHF + zod-resolver as peer deps
- Server validation_failed → setError per field
- Hydrate-once semantics (ref-guarded)

N tests cover defaults/hydrate/submit/serverErrors/reset paths.
```

codex
I’ll implement this in the worktree, starting with the repo’s required scope-discipline doc and then reading the existing TS runtime patterns so the adapter matches local conventions.
  → Read required repo guidance and existing runtime patterns
  • Implement RHF adapter and package exports/deps
  • Add tests and STATUS sample
  • Run focused verification and summarize outcome
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem runtime/ts/lazuli/src' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content docs/scope-discipline.md' in c:\tmp\migrate-rhf-adapter
 succeeded in 292ms:

    Directory: C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src

[32;1mMode   [0m[32;1m              LastWriteTime[0m[32;1m         Length[0m[32;1m Name[0m
[32;1m----   [0m [32;1m             -------------[0m [32;1m        ------[0m [32;1m----[0m
-a---          22/05/2026    22:28           3194 case-mapper.ts
-a---          22/05/2026    22:28           7713 client.refresh.test.ts
-a---          22/05/2026    22:28           9221 client.ts
-a---          22/05/2026    22:28           2543 drawer-sub-view.native.ts
-a---          22/05/2026    22:28           2640 drawer-sub-view.web.ts
-a---          22/05/2026    22:28           3461 error.ts
-a---          22/05/2026    22:28           3073 exports-parity.test.ts
-a---          22/05/2026    22:28            671 flash-adapter.ts
-a---          22/05/2026    22:28           3141 formatters.test.ts
-a---          22/05/2026    22:28           7566 formatters.ts
-a---          22/05/2026    22:28           1740 index.ts
-a---          22/05/2026    22:28           1993 lifecycle-gate.tsx
-a---          22/05/2026    22:28           2360 local-setting.native.ts
-a---          22/05/2026    22:28           1631 local-setting.web.test.ts
-a---          22/05/2026    22:28           3298 local-setting.web.ts
-a---          22/05/2026    22:28           2525 message-for-error.test.ts
-a---          22/05/2026    22:28           1139 message-for-error.ts
-a---          22/05/2026    22:28           5544 react.native.ts
-a---          22/05/2026    22:28           9655 react.ts
-a---          22/05/2026    22:28           5742 react.web.ts
-a---          22/05/2026    22:28           2182 route-guard-component.tsx
-a---          22/05/2026    22:28           1552 route-guard.test.ts
-a---          22/05/2026    22:28           3740 route-guard.ts
-a---          22/05/2026    22:28            687 router-adapter.ts
-a---          22/05/2026    22:28            688 scalars.ts
-a---          22/05/2026    22:28           3668 spec.ts
-a---          22/05/2026    22:28            919 tanstack-adapter.ts
-a---          22/05/2026    22:28           3191 types.ts
-a---          22/05/2026    22:28           1591 use-actor.test.tsx
-a---          22/05/2026    22:28            991 use-actor.ts
-a---          22/05/2026    22:28           8098 use-lazuli-action.test.tsx
-a---          22/05/2026    22:28           4922 use-lazuli-action.ts
-a---          22/05/2026    22:28           4019 use-lazuli-command.invalidates.test.tsx
-a---          22/05/2026    22:28           8142 use-lazuli-form.test.tsx
-a---          22/05/2026    22:28           5870 use-lazuli-form.ts
-a---          22/05/2026    22:28           1697 use-lazuli-route-params.test.tsx
-a---          22/05/2026    22:28           1787 use-lazuli-route-params.ts
-a---          22/05/2026    22:28           3737 view-helpers.test.ts
-a---          22/05/2026    22:28          11449 view-helpers.ts


 succeeded in 309ms:
{
  "name": "@lazuli/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Lazuli universal runtime — typed client + React hooks (TanStack Query). One package serves web and React Native via the exports map's `react-native` condition; see docs/proposals/mobile-target.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./react": {
      "types": "./src/react.ts",
      "react-native": "./src/react.native.ts",
      "default": "./src/react.web.ts"
    },
    "./react/tanstack": {
      "types": "./src/tanstack-adapter.ts",
      "import": "./src/tanstack-adapter.ts",
      "default": "./src/tanstack-adapter.ts"
    },
    "./formatters": {
      "types": "./src/formatters.ts",
      "import": "./src/formatters.ts",
      "default": "./src/formatters.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "tsc": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "search-query-parser": "^1.6.0"
  },
  "peerDependencies": {
    "@react-native-async-storage/async-storage": ">=1.23.0",
    "@tanstack/react-query": ">=5.0.0",
    "@tanstack/react-router": ">=1.0.0",
    "react": ">=18.0.0",
    "react-native": ">=0.74.0"
  },
  "peerDependenciesMeta": {
    "@react-native-async-storage/async-storage": { "optional": true },
    "@tanstack/react-query": { "optional": true },
    "@tanstack/react-router": { "optional": true },
    "react": { "optional": true },
    "react-native": { "optional": true }
  },
  "devDependencies": {
    "@tanstack/react-query": "^5.59.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "happy-dom": "^15.11.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}

 succeeded in 317ms:
# Scope Discipline — What Lazuli Owns vs What Apps Own

**Status**: canonical operating policy. Mirrored in `CLAUDE.md` / `AGENTS.md` for AI agents and in the project's contributor guidelines for humans.

**Audience**: language designers (proposals, IR, codegen), runtime maintainers, plugin authors, and downstream app authors.

**Date**: 2026-05-14.

## TL;DR

Lazuli aims to absorb **~80% of any production-grade backend** through generic primitives. The remaining **~20% is app-specific** and belongs in **handlers, escape hatches, or plugins** — not in the framework.

We are not chasing every demand of every downstream consumer. We will not absorb per-vendor adapters, per-domain scalars, per-country regulations, or per-product UX flows into the core. Those are an app's job.

This rule has teeth: any proposal that tries to make the framework conform to a single app's specifics is a **scope violation** and will be rejected (or kicked to `@plugin/<name>`).

The negative reference is **the framework lineage that preceded Lazuli** (template-driven full codegen that grew unbounded as every client demand was absorbed). Lazuli exists to NOT repeat that.

## The 80/20 boundary

### What the framework owns (the generic 80%)

Closed-catalog primitives shared across virtually every production-grade backend:

- **Resources** (data model, types, relations, soft-delete, retention, multi-tenancy).
- **Commands** (write effects, idempotency, audit, lifecycle transitions).
- **Queries** (list/lookup/sql, filters, pagination, cache, search).
- **Lifecycle** (state machines on resources, terminal states, invariants).
- **Jobs** (queued or scheduled, retry, backoff, idempotency).
- **Webhooks** (HMAC-verified inbound, envelope, dispatch).
- **Pollers** (async cursor loops, retry, terminal status).
- **Notifications** (channel-agnostic dispatch).
- **APIs** (typed HTTP routes, policy, rate_limit).
- **Reports** (CSV/XLSX export with signed URLs).
- **Agents** (LLM contract surface).
- **Storage** (object stores, signed URLs, blob upload).
- **Encryption at rest** (AES-256-GCM field-level cipher).
- **RBAC** (role/permission catalogs with closure-baked codegen).
- **Plans + gates** (subscription tiers, feature gates, quota gates).
- **Multi-tenancy** (tenant pinning, row scoping, audit trail).
- **i18n / translation**.
- **Migrations** (auto-emit DDL).
- **Observability** (tracing, logging, audit).
- **Auth** (session, password, MFA basics).

Anything in this catalog is **the framework's responsibility**. New entries enter only when they (a) generalize to ≥3 distinct app shapes and (b) survive the architect-grading rubric (≥ 8.5 / no dim < 7) via an architect-graded proposal in the operational archive.

### What apps own (the specific 20%)

Specifics that vary per app, per client, per industry, or per geography. Apps own these via the **five escape hatches** documented below.

Examples — **non-exhaustive**, listed only to make the boundary concrete:

- Per-vendor API integrations (any specific SaaS provider's HTTP client).
- Per-vendor webhook payload schemas + HMAC quirks.
- Per-vendor async-resolution quirks (gender flip, retry-after with custom headers, etc.).
- Per-country scalars (national ID formats, phone formats, address layouts, locale-specific currency parsing).
- Per-domain UI flows (specific paywall layouts, specific onboarding wizards).
- Per-business-rule logic (this company's commission table, this product's discount tree).
- Per-deploy-target configuration (Procfile shape, container build, k8s manifests).
- One-time port tooling (migrating from a specific legacy ORM).
- Custom analytics dashboards / report column expansions specific to one product.

If a downstream consumer wants any of the above, the answer is **"write a handler"** or **"author a plugin"** — not "extend the framework grammar."

### What plugins own (the shared-but-not-canonical middle)

Reusable code that's **broader than one app** but **not canonical enough for core**:

- `@plugin/<saas-provider>` adapters for paid commodities (payment processors, observability vendors, email providers, push providers, feature-flag managers).
- `@plugin/scalars-<locale>` kits (national ID / phone / address / currency parsers for one locale).
- `@plugin/<reusable-widget>` for cross-app frontend components (e.g., a tax-id input field, a signature pad).
- Canonical kit-packs that demonstrate a recurring pattern (e.g., a starter CRM block).

Plugins live in **separate repos** at `github.com/lazuli-lang/lazuli-plugin-<name>` (public) or under the consuming organization (private). See `docs/plugin-authoring.md`.

## The five escape hatches

When an app needs something the framework doesn't model, these five mechanisms cover it:

1. **`@fn.<name>` handlers**. Every command/query/job/webhook/poller surface accepts `@fn.<name>` references that lower to user-written Go in `features/<feature>/handlers/<name>.go`. This is the primary escape hatch. Examples: vendor API call, custom validation, derived field computation, multi-step orchestration.

2. **Opaque `handler "./path.go"` on `api` blocks**. For full-control endpoints that don't fit the typed `command`/`query` shape (e.g., a vendor's bespoke webhook callback with three nested polymorphic envelopes), declare `api foo / handler "./api/foo.go"`. The generated server mounts the route; the handler owns the body.

3. **`query.sql` raw blocks**. For aggregations, joins, window functions, or any SQL the typed `query.list` / `query.lookup` shape doesn't cover, declare `query.sql foo / @file.foo.sql`. The framework wires the route and policy; the SQL is yours.

4. **`extends @anchor` / `slot`** view extensibility. Frontend cells, drawers, list columns, and detail panels can be extended by sibling features via `extends @anchor.<name> / slot <position> / block @client.<widget>`. App owns the widget; framework owns the slotting.

5. **User-owned `main.go`** (or `dist/go/main.go` overrides). Generated `main.go` is replaceable. For runtime topology decisions — which subset of jobs to register per worker process, custom middleware ordering, integration-test hooks — the app's `main.go` (or a wrapper around the generated entry point) is the contract surface.

Together these cover every specific the framework doesn't model. **There is no sixth escape hatch coming**; if a need can't be expressed via these five, the framework grammar likely needs to grow (and that goes through a proposal).

## Decision tree

When you encounter a gap (an app can't express what it needs):

```
Is it generic across ≥3 distinct app shapes?
├─ YES → propose framework adoption.
│   ├─ Architect-graded proposal (target ≥ 8.5).
│   ├─ Surface + IR + analyzer + doctor + codegen + runtime.
│   └─ Lands in core.
└─ NO → is it a commodity SaaS / locale kit / reusable widget?
    ├─ YES → ship as `@plugin/<name>` in a separate repo.
    │   └─ See `docs/plugin-authoring.md`.
    └─ NO → it's app-specific.
        └─ Use one of the five escape hatches.
```

The "≥3 distinct app shapes" gate is the most-cited rejection criterion. Pilot evidence from one product does not justify framework adoption.

## What's explicitly out of scope

The framework will NOT absorb (today, and as a matter of policy):

- Specific named SaaS providers (payments, email, observability, feature flags, maps). These belong in plugins.
- Specific national/locale identifiers (any country's tax id, phone format, address parser). These belong in `@plugin/scalars-<locale>`.
- Specific vendor webhook schemas, HMAC variations, or rate-limit quirks. These belong in handlers.
- Specific UI flows (paywall layouts, onboarding wizards, branded checkout). Apps own UX.
- Specific business-domain logic (commission tables, discount trees, fraud heuristics). Apps own domain logic.
- Specific deploy targets (Procfile, k8s manifest, container build). Apps own ops.
- Specific frontend libraries' API shapes (a particular component-library's button props). Plugins or apps own these.
- One-time legacy-port tooling (migrating from a specific predecessor ORM). One-off scripts, not framework features.
- **CRDT / collaborative document sync** (Y.js, Hocuspocus, Automerge, server-side operational transform). 3-OSS evidence (Plane Hocuspocus + ToolJet Yjs gateway + AppFlowy CRDT-block) **confirms the plugin boundary holds**, not that it should move. See "no CRDT in core" boundary affirmation below.

If you find yourself writing IR types or doctor rules named after a specific vendor, country, or product — you're outside the framework's scope. Stop.

## Principle — static rejection over runtime guard (2026-05-17)

Codified per architect re-grade observation #4 — two Wave 1+2 proposals
independently applied this discipline:

- `record-trait-tree.md` §3 rejects runtime cycle guards in favor of
  a static doctor lint (`VOCAB-TREE-001`, proposal-pending) that walks
  the parent FK graph at parse/IR time. The AppFlowy negative signal
  (audit pattern #4) confirmed runtime guards are exactly the wrong
  shape.
- `datasource-plugin-contract.md` §3.4 rejects multi-parent `extends`
  because diamond resolution would require runtime chain-walking;
  cycles in single-parent `extends` (`A extends B extends A`) are
  rejected statically by the manifest validator before codegen runs.

The rule (canonical from 2026-05-17 forward):

> **Any class of structural invariant that can be checked at parse,
> IR, or doctor time MUST be rejected statically. Runtime guards are
> reserved for invariants that genuinely depend on runtime state —
> e.g. foreign-key constraint violations on concurrent writes, race
> conditions between sessions, external-service availability.**

Structural invariants that fall under this rule (non-exhaustive):

- Cycle detection in self-referential resources (`tree`, `extends`
  chains).
- Cardinality bounds in closed-catalog enums (`channel`, `kind`).
- Type compatibility between bindings and target fields.
- Namespace closure (a referenced `@plugin/*` must be in the
  registry).
- Audience drift across composed resources (parent vs child,
  versioned table vs source).

Runtime-state invariants that legitimately need runtime guards:

- Foreign-key integrity under concurrent writes (DB enforces).
- Optimistic-lock version mismatches.
- Rate-limit bucket exhaustion.
- External-service authentication failures.
- Session-validity at request time.

If a proposal needs a runtime guard for a class that COULD be checked
statically, it must justify the runtime path explicitly and amend
this section. The default is static.

## Boundary affirmation — no CRDT in core (2026-05-17)

The OSS Mirror Wave 1+2 audit (`lazuli-ops/docs/proposals/synth-oss-mirror-wave-1-2-2026-05-17.md` §Theme 2 + §3.E) surfaced three independent products implementing CRDT-style document sync:

| Product | Library | Audit reference |
|---|---|---|
| Plane | Hocuspocus (Y.js-based) | `apps/live/src/hocuspocus.ts` — audit §pattern #12 |
| ToolJet | Yjs gateway | `server/src/modules/events/yjs.gateway.ts` — audit §pattern #12 |
| AppFlowy | CRDT block + awareness | `flowy-document/` cluster — audit §patterns #5, #8, #11, #12, #18 |

Naive cross-pilot inference would read this as "3-OSS confirms Y.js belongs in core". The opposite is correct: **when three independent products use the same external library with the same plugin-adapter pattern, that is evidence the plugin boundary is correctly placed, not evidence the boundary should move.** Y.js is mature, vendor-stable, single-vendor, and the wire is well-known — exactly the shape `@plugin/*` was designed for.

**The rule (canonical from 2026-05-17 forward):**

> CRDT-style collaborative document sync (operational transform, conflict-free replicated data types, awareness/presence streams, doc-version vectors, undo/redo over replicated state) does NOT enter Lazuli core grammar. It lives in `@plugin/yjs`, `@plugin/hocuspocus`, `@plugin/automerge`, or product-specific plugin namespaces. The `channel <name>` primitive (`lazuli-ops/docs/proposals/bucket-realtime-scope.md`) is the closest the framework gets — typed messages over a tenant-scoped channel, NOT replicated document state.

This rule applies even when:
- A pilot ships CRDT functionality via plugin (the plugin is the answer; do not "graduate" it into core).
- A fourth, fifth, or Nth OSS product adds CRDT (the rule's premise is shape-stability, not popularity).
- Lazuli's own future tooling needs collaborative editing for its own DSL (the LSP/editor uses `@plugin/*` like any other consumer).

The boundary affirmation is a **deletion**, not a deferral. The previously-deferred `presence` / `coedit channel kind` / `doc_sync` entries in `bucket-realtime-scope.md` MVP are not "waiting for ≥3 confirmations"; they are **rejected on principle** and the 3-OSS evidence is the citation.

If a future proposal contradicts this rule, the proposal must cite this section, articulate why the wire-thin principle no longer applies, and re-grade `scope-discipline.md` itself — not merely add a sibling primitive.

## When the boundary moves

The boundary is not frozen. Things can graduate **into** the framework when:

1. **Pilot evidence shows generalization**: ≥3 distinct downstream apps independently produce the same workaround pattern, in shapes that compose. (One app's pressure is not enough; that's the most common mistake.)
2. **The workaround in escape hatches is visibly painful**: not "we could do it in a handler" but "every consumer is duplicating the same 200 LOC handler with the same bugs."
3. **A clean closed-catalog form exists**: the abstraction passes the cold-readability test (an LLM or a new contributor can author it from the surface alone, without reading framework internals).
4. **An architect-graded proposal lands**: the `<name>` proposal (operational archive) ≥ 8.5 with no dimension < 7. See `feedback_grade_before_commit.md`.

Conversely, things can be **demoted out** of the framework when:

- A primitive shipped on speculation collects zero real usage after two production-app pilots.
- A primitive turns out to encode one client's specifics in framework dress (this is the most dangerous case — visible only in retrospect; the audit is `git log` + namespace policy violations).

**Open invitation**: if you have evidence that something currently in an escape hatch is generic enough to belong in the framework, write a proposal. Don't add ad-hoc adoption. The architect-grading loop exists to keep the bar honest.

## The negative reference

The framework lineage that preceded Lazuli (template-driven full codegen) became unmaintainable specifically because **every downstream client's demand was absorbed into the template**. The codebase grew to embed every payment processor, every webhook flavor, every locale's tax id, every country's address format. Eventually the templates carried more vendor-specific code than generic infrastructure, and the maintenance cost exceeded the value.

Lazuli's discipline: **the templates carry the generic primitives; the wires carry external code; vendor specifics live in plugins or apps**. The wire-thin principle in `CLAUDE.md` is the operational version of this rule.

If a proposal feels like it's chasing a specific client's specifics — even ours, even a paying one — push back. The framework's value is in being generic. Surrendering that to absorb specifics is the failure mode that already killed one framework.

## Cross-references

- `CLAUDE.md` / `AGENTS.md` — operating manual; namespace policy + wire-thin discipline.
- `docs/design-principles.md` — Rule Zero ("Vocabulary Over Mechanism").
- `docs/invariants.md` — closed grammar/IR catalogs.
- `docs/plugin-authoring.md` — how to ship a plugin.
- the `production-readiness` proposal (operational archive) — meta-roadmap with status per gap (framework / plugin / handler).
- Per-feature proposals in the `*-vocab` proposal (operational archive) — examples of architect-graded scope decisions.

codex
The repo guidance confirms this adapter is a generic frontend integration surface, not a provider-specific runtime addition. I’m reading the existing React hooks and test setup next so the new subpath behaves like the rest of the package.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/use-lazuli-form.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/use-lazuli-form.test.tsx' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/react.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/spec.ts' in c:\tmp\migrate-rhf-adapter
 succeeded in 285ms:
import { useCallback, useEffect, useMemo, useRef, useState } from "react";

import { isLazuliError } from "./error.js";
import type { CommandSpec, QuerySpec } from "./spec.js";

type EmptyArgs = Record<string, never>;

const emptyArgs: EmptyArgs = {};
const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
  kind: "query",
  name: "__lazuli.form.disabled",
};

type QueryHook = <Result>(
  spec: QuerySpec<EmptyArgs, Result>,
  args: EmptyArgs,
  options?: { enabled?: boolean },
) => { data: Result | undefined };

type CommandHook = <Input, Output>(
  spec: CommandSpec<Input, Output>,
) => {
  mutateAsync: (input: Input) => Promise<Output>;
  isPending: boolean;
  error: unknown;
  reset: () => void;
};

export type UseLazuliFormOptions<Values extends object, SubmitInput, SubmitOutput> = {
  defaults: Values;
  hydrateFrom?: QuerySpec<EmptyArgs, Partial<Values>>;
  submit: CommandSpec<SubmitInput, SubmitOutput>;
  mapValuesToInput: (values: Values) => SubmitInput;
  onSuccess?: (out: SubmitOutput, values: Values) => void;
  onError?: (err: unknown, values: Values) => void;
};

export type UseLazuliFormResult<Values extends object> = {
  values: Values;
  setValue: <K extends keyof Values>(field: K, value: Values[K]) => void;
  setValues: (next: Partial<Values>) => void;
  reset: () => void;
  handleSubmit: () => Promise<void>;
  isDirty: boolean;
  isSubmitting: boolean;
  error: unknown;
  serverErrors: Partial<Record<keyof Values, string>>;
};

export function createUseLazuliForm(useQuery: QueryHook, useCommand: CommandHook) {
  return function useLazuliForm<Values extends object, SubmitInput, SubmitOutput>(
    opts: UseLazuliFormOptions<Values, SubmitInput, SubmitOutput>,
  ): UseLazuliFormResult<Values> {
    const [values, setFormValues] = useState<Values>(opts.defaults);
    const [serverErrors, setServerErrors] = useState<Partial<Record<keyof Values, string>>>({});
    const hydratedRef = useRef(false);

    const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
      EmptyArgs,
      Partial<Values>
    >;
    const hydrate = useQuery(hydrateSpec, emptyArgs, { enabled: opts.hydrateFrom !== undefined });
    const command = useCommand(opts.submit);

    useEffect(() => {
      if (hydratedRef.current || hydrate.data === undefined) return;
      hydratedRef.current = true;
      setFormValues((current) => ({ ...current, ...hydrate.data }) as Values);
    }, [hydrate.data]);

    const setValue = useCallback(
      <K extends keyof Values>(field: K, value: Values[K]) => {
        setFormValues((current) => ({ ...current, [field]: value }) as Values);
        setServerErrors((current) => omitServerErrors(current, [field]));
      },
      [],
    );

    const setValues = useCallback((next: Partial<Values>) => {
      setFormValues((current) => ({ ...current, ...next }) as Values);
      setServerErrors((current) =>
        omitServerErrors(current, Object.keys(next) as Array<keyof Values>),
      );
    }, []);

    const reset = useCallback(() => {
      setFormValues(opts.defaults);
      setServerErrors({});
      command.reset();
    }, [command, opts.defaults]);

    const handleSubmit = useCallback(async () => {
      setServerErrors({});
      const submittedValues = values;
      try {
        const out = await command.mutateAsync(opts.mapValuesToInput(submittedValues));
        opts.onSuccess?.(out, submittedValues);
      } catch (err) {
        setServerErrors(extractServerErrors(err, submittedValues));
        opts.onError?.(err, submittedValues);
      }
    }, [command, opts, values]);

    const isDirty = useMemo(
      () => JSON.stringify(values) !== JSON.stringify(opts.defaults),
      [opts.defaults, values],
    );

    return {
      values,
      setValue,
      setValues,
      reset,
      handleSubmit,
      isDirty,
      isSubmitting: command.isPending,
      error: command.error,
      serverErrors,
    };
  };
}

function omitServerErrors<Values extends object>(
  current: Partial<Record<keyof Values, string>>,
  fields: ReadonlyArray<keyof Values>,
): Partial<Record<keyof Values, string>> {
  let changed = false;
  const next = { ...current };
  for (const field of fields) {
    if (field in next) {
      delete next[field];
      changed = true;
    }
  }
  return changed ? next : current;
}

function extractServerErrors<Values extends object>(
  err: unknown,
  values: Values,
): Partial<Record<keyof Values, string>> {
  if (!isLazuliError(err) || err.code !== "validation_failed") return {};
  const fields = asRecord(asRecord(err.data).fields);
  const out: Partial<Record<keyof Values, string>> = {};
  for (const [rawField, rawMessage] of Object.entries(fields)) {
    const message = fieldMessage(rawMessage);
    if (message) out[normalizeFieldName(rawField, values)] = message;
  }
  return out;
}

function fieldMessage(value: unknown): string | null {
  if (typeof value === "string") return value;
  if (Array.isArray(value)) {
    for (const item of value) {
      const nested = fieldMessage(item);
      if (nested) return nested;
    }
    return null;
  }
  const record = asRecord(value);
  for (const key of ["message", "reason", "code"] as const) {
    const candidate = record[key];
    if (typeof candidate === "string") return candidate;
  }
  return null;
}

function normalizeFieldName<Values extends object>(raw: string, values: Values): keyof Values {
  const record = values as Record<PropertyKey, unknown>;
  if (raw in record) return raw as keyof Values;
  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
  return (camel in record ? camel : raw) as keyof Values;
}

function asRecord(value: unknown): Record<string, unknown> {
  return value !== null && typeof value === "object" && !Array.isArray(value)
    ? (value as Record<string, unknown>)
    : {};
}

 succeeded in 710ms:
// PUBLIC TYPE CONTRACT for `@lazuli/runtime/react`.
//
// This file is the `types` branch of the `./react` exports map in
// `package.json`. Both `react.web.ts` and `react.native.ts` must
// re-export every name listed here so the consumer sees a uniform
// surface regardless of platform. The runtime body lives in those
// concrete entrypoints; this file declares the contract that both
// implementations satisfy.
//
// Constraint: this file must NOT import `react-native` modules even
// at type position. Web consumers running `tsc --noEmit` without RN
// installed should typecheck cleanly. Inline type aliases (or
// declarations of the public surface) replace any would-be RN imports.
//
// See `docs/proposals/mobile-target.md` §3.3 + §3.5 for the rationale.

import type {
  UseMutationOptions,
  UseMutationResult,
  UseQueryOptions,
  UseQueryResult,
} from "@tanstack/react-query";
import type { ComponentType, ReactNode } from "react";

import type { LazuliClient, LazuliRouter } from "./client.js";
import type { UseLazuliActionOptions, UseLazuliActionResult } from "./use-lazuli-action.js";
import type {
  LifecycleGateEvaluator,
  LifecycleGateMetadata,
  LifecycleVerdict,
} from "./lifecycle-gate.js";
import type {
  LazuliActor,
  LazuliRouteGuardPolicy,
  RouteGuardVerdict,
} from "./route-guard.js";
import type { CommandSpec, QuerySpec } from "./spec.js";
import type { UseLazuliFormOptions, UseLazuliFormResult } from "./use-lazuli-form.js";
import type {
  RouteParamParser,
  UseLazuliRouteParamsOptions,
} from "./use-lazuli-route-params.js";
import type { DrawerConfig, DrawerSubView } from "./view-helpers.js";

// Universal view-helper types and functions are re-exported as-is from
// `view-helpers.ts`; they are pure React + JS (no DOM, no RN) and so
// both entrypoints can re-export them identically through this file.
export {
  canonicalizeSearch,
  parseSegments,
  useFilterState,
  useMultiSelection,
  type DrawerConfig,
  type DrawerSubView,
  type FilterConfig,
  type FilterState,
  type FilterStates,
  type MultiFilterState,
  type MultiSelection,
  type ParsedSegment,
  type SetUrlParams,
  type UrlParams,
} from "./view-helpers.js";

// --- LazuliProvider / client hooks ----------------------------------------

export interface LazuliProviderProps {
  client: LazuliClient;
  children: ReactNode;
}

export declare function LazuliProvider(props: LazuliProviderProps): ReactNode;

export declare function useLazuliClient(override?: LazuliClient): LazuliClient;

/**
 * Build the TanStack Query cache key for a typed query. Splitting the
 * prefix `["lazuli", spec.name]` from the args lets command hooks
 * invalidate a whole query (across every args set) by passing
 * `{ queryKey: ["lazuli", spec.name] }`.
 */
export declare function queryKeyFor(
  spec: QuerySpec<unknown, unknown>,
  args: unknown,
): unknown[];

export type UseLazuliQueryOptions<Result> = Omit<
  UseQueryOptions<Result, Error, Result, unknown[]>,
  "queryKey" | "queryFn"
> & {
  client?: LazuliClient;
};

export declare function useLazuliQuery<Args, Result>(
  spec: QuerySpec<Args, Result>,
  args: Args,
  options?: UseLazuliQueryOptions<Result>,
): UseQueryResult<Result, Error>;

export type UseLazuliCommandOptions<Input, Output> = Omit<
  UseMutationOptions<Output, Error, Input>,
  "mutationFn"
> & {
  client?: LazuliClient;
};

export declare function useLazuliCommand<Input, Output>(
  spec: CommandSpec<Input, Output>,
  options?: UseLazuliCommandOptions<Input, Output>,
): UseMutationResult<Output, Error, Input>;

export type {
  UseLazuliActionOptions,
  UseLazuliActionResult,
};

export {
  LazuliFlashAdapterContext,
  LazuliFlashAdapterProvider,
  type FlashAdapter,
  type FlashKind,
} from "./flash-adapter.js";

export {
  LazuliRouterAdapterContext,
  LazuliRouterAdapterProvider,
  type RouterAdapter,
} from "./router-adapter.js";

export declare function useLazuliAction<Input, Output>(
  spec: CommandSpec<Input, Output>,
  options?: UseLazuliActionOptions<Input, Output>,
): UseLazuliActionResult<Input, Output>;

export type { UseLazuliFormOptions, UseLazuliFormResult };

export declare function useLazuliForm<Values extends object, Input, Output>(
  opts: UseLazuliFormOptions<Values, Input, Output>,
): UseLazuliFormResult<Values>;

export {
  LazuliRouteParamsError,
  isLazuliRouteParamsError,
  type RouteParamParser,
  type UseLazuliRouteParamsOptions,
} from "./use-lazuli-route-params.js";

export declare function useLazuliRouteParams<T>(
  viewName: string,
  parser: RouteParamParser<T>,
  raw: Record<string, string>,
  options?: UseLazuliRouteParamsOptions,
): T;

// --- Route guards ---------------------------------------------------------

export type {
  LazuliActor,
  LazuliRouteGuardPolicy,
  RouteGuardVerdict,
};

export type RouteGuardSpec<Component = unknown> = {
  readonly path?: string;
  readonly component?: Component;
  readonly policy: LazuliRouteGuardPolicy;
  readonly onUnauthenticated?: string | null;
  readonly onUnauthorized?: string | null;
  readonly substep?: string;
};

export type RouteGuard = RouteGuardSpec<unknown>;
export type ActorQueryRef = string;
export type RouteGuardRegistry = {
  readonly defaults: {
    readonly policy: LazuliRouteGuardPolicy | null;
    readonly unauthenticated: string | null;
    readonly unauthorized: string | null;
    readonly skeleton: string | null;
  };
  readonly actorQuery: ActorQueryRef | null;
  readonly routes: Readonly<Record<string, RouteGuard>>;
};

/**
 * TanStack Router beforeLoad support is intentionally split out at
 * `@lazuli/runtime/react/tanstack` so plain React imports do not load
 * `@tanstack/react-router`.
 */

export type UseActorResult = {
  readonly data: LazuliActor | null;
  readonly isLoading: boolean;
  readonly isError: boolean;
};

export declare function evaluatePolicy(
  actor: LazuliActor | null,
  policy: LazuliRouteGuardPolicy,
): RouteGuardVerdict;

export type WithTanStackGuardOptions = {
  readonly onUnauthenticated?: string | null;
  readonly onUnauthorized?: string | null;
  readonly redirect: (params: { to: string }) => unknown;
};

/**
 * TanStack Router beforeLoad guard. Caller passes the `redirect` import
 * from `@tanstack/react-router` so `@lazuli/runtime/react` itself does
 * NOT take a hard dependency on the router (consumers without TanStack
 * don't pay the bundle cost). The router context must carry the
 * LazuliClient on `context.client`.
 */
export declare function withTanStackGuard<TContext extends { client: LazuliClient }>(
  phantomContext: Partial<TContext>,
  policy: LazuliRouteGuardPolicy,
  options: WithTanStackGuardOptions,
): (params: { context: TContext }) => Promise<void>;

export declare function useActor(): UseActorResult;

export interface RouteGuardProps {
  readonly policy: LazuliRouteGuardPolicy;
  readonly onUnauthenticated?: string | null;
  readonly onUnauthorized?: string | null;
  readonly children: ReactNode;
  readonly skeleton?: ReactNode;
  readonly router?: LazuliRouter | null;
}

export declare function RouteGuard(props: RouteGuardProps): ReactNode;

export type RouteGuardOptions = Omit<RouteGuardProps, "children">;

export declare function useRouteGuardSkeleton(): ReactNode;

export declare function withRouteGuard<P extends object>(
  Component: ComponentType<P>,
  guard: RouteGuardOptions,
): ComponentType<P>;

// --- Lifecycle route gates -----------------------------------------------

export type {
  LifecycleGateEvaluator,
  LifecycleGateMetadata,
  LifecycleVerdict,
};

export declare function useLifecycleGate(
  metadata: LifecycleGateMetadata,
  evaluator: LifecycleGateEvaluator,
): { verdict: LifecycleVerdict | "hydrating" };

export declare function withLifecycleGate<P extends object>(
  Component: ComponentType<P>,
  metadata: LifecycleGateMetadata,
  evaluator: LifecycleGateEvaluator,
): ComponentType<P>;

// --- Platform-split hooks --------------------------------------------------

/**
 * Returns the persisted setting, falling back to `defaultValue`.
 *
 * IMPORTANT — first-render contract differs by platform:
 *   - Web: returns the persisted value synchronously on first render
 *     (uses useSyncExternalStore + localStorage).
 *   - Native: returns `defaultValue` on first render; the persisted
 *     value becomes visible on the next render after AsyncStorage
 *     resolves.
 *
 * Callers MUST treat the value as eventually-consistent. Code that
 * depends on the persisted value being present on the first frame
 * (e.g., to pick a theme before paint) must read storage explicitly
 * via the platform's lower-level API, not via this hook.
 */
export declare function useLocalSetting<T>(
  key: string,
  defaultValue: T,
): [T, (next: T) => void];

/**
 * Drawer state machine. Auto-closes on pathname change, on delete
 * success, on missing item, and on a platform-specific dismissal
 * gesture:
 *   - Web: the `Escape` keyboard key.
 *   - Native: the Android hardware back button (iOS swipe-back is
 *     handled by Expo Router for navigated stack screens; `<Modal>`
 *     consumers wire `onRequestClose` to `close()` themselves).
 */
export declare function useDrawerSubView<TInput, TItem>(
  config?: DrawerConfig<TInput, TItem>,
): DrawerSubView<TInput, TItem>;

// React + react-query primitives re-exported via @lazuli/runtime/react so
// generated codegen (cap_file hooks) doesn't import raw `react` /
// `@tanstack/react-query` modules from dist file locations that can't
// resolve them through the pilot's per-app node_modules.
export { useCallback, useState } from "react";
export { useQueryClient } from "@tanstack/react-query";

 succeeded in 852ms:
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { act, renderHook, waitFor } from "@testing-library/react";
import type { ReactNode } from "react";
import { describe, expect, it, vi } from "vitest";

import { LazuliClient } from "./client.js";
import { LazuliProvider, useLazuliForm } from "./react.web.js";
import { defineCommand, defineQuery } from "./spec.js";

type ProfileValues = {
  fullName: string;
  email: string;
  age: number;
};

const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");

function makeHarness(fetchImpl?: typeof globalThis.fetch) {
  const client = new LazuliClient({
    baseUrl: "http://runtime.test",
    fetch: fetchImpl ?? okFetch({ ok: true }),
  });
  const queryClient = new QueryClient({
    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
  });
  const wrapper = ({ children }: { children: ReactNode }) => (
    <QueryClientProvider client={queryClient}>
      <LazuliProvider client={client}>{children}</LazuliProvider>
    </QueryClientProvider>
  );
  return { queryClient, wrapper };
}

function okFetch(body: unknown): typeof globalThis.fetch {
  return async () => jsonResponse(body);
}

function jsonResponse(body: unknown, status = 200): Response {
  return new Response(JSON.stringify(body), { status });
}

describe("useLazuliForm", () => {
  it("uses defaults as initial values", () => {
    const { wrapper } = makeHarness();

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
        }),
      { wrapper },
    );

    expect(result.current.values).toEqual(defaults);
    expect(result.current.isDirty).toBe(false);
    expect(result.current.serverErrors).toEqual({});
  });

  it("merges hydrateFrom query data into the form once loaded", async () => {
    const { wrapper } = makeHarness(async (input) => {
      expect(String(input)).toContain("/api/v1/q/profile.lookup");
      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
    });

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          hydrateFrom: hydrateProfile,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
        }),
      { wrapper },
    );

    await waitFor(() => expect(result.current.values.fullName).toBe("Ada Lovelace"));
    expect(result.current.values).toEqual({
      fullName: "Ada Lovelace",
      email: "ada@example.com",
      age: 0,
    });
  });

  it("hydrates only once so refetches do not overwrite user edits", async () => {
    let queryCalls = 0;
    const { queryClient, wrapper } = makeHarness(async () => {
      queryCalls += 1;
      return jsonResponse({
        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
      });
    });

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          hydrateFrom: hydrateProfile,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
        }),
      { wrapper },
    );

    await waitFor(() => expect(result.current.values.fullName).toBe("Ada Lovelace"));

    act(() => result.current.setValue("fullName", "Typed Name"));
    await act(async () => {
      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
    });

    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
    expect(result.current.values.fullName).toBe("Typed Name");
  });

  it("updates fields and marks the form dirty", () => {
    const { wrapper } = makeHarness();

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
        }),
      { wrapper },
    );

    act(() => result.current.setValue("fullName", "Ada Lovelace"));
    expect(result.current.values.fullName).toBe("Ada Lovelace");
    expect(result.current.isDirty).toBe(true);

    act(() => result.current.setValues({ email: "ada@example.com", age: 36 }));
    expect(result.current.values).toEqual({
      fullName: "Ada Lovelace",
      email: "ada@example.com",
      age: 36,
    });
  });

  it("submits mapped values through the command hook", async () => {
    const requests: unknown[] = [];
    const onSuccess = vi.fn();
    const { wrapper } = makeHarness(async (_input, init) => {
      requests.push(JSON.parse(String(init?.body)));
      return jsonResponse({ ok: true });
    });

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          submit: submitProfile,
          mapValuesToInput: (values) => ({
            fullName: values.fullName.trim(),
            email: values.email,
            age: values.age,
          }),
          onSuccess,
        }),
      { wrapper },
    );

    act(() => result.current.setValues({ fullName: "  Ada Lovelace  ", age: 36 }));
    await act(async () => {
      await result.current.handleSubmit();
    });

    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
    expect(onSuccess).toHaveBeenCalledWith(
      { ok: true },
      { fullName: "  Ada Lovelace  ", email: "", age: 36 },
    );
  });

  it("surfaces validation_failed field errors from the server", async () => {
    const onError = vi.fn();
    const { wrapper } = makeHarness(async () =>
      jsonResponse(
        {
          code: "validation_failed",
          message: "invalid profile",
          data: {
            fields: {
              full_name: "too_short",
              email: { message: "invalid_format" },
            },
          },
        },
        400,
      ),
    );

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
          onError,
        }),
      { wrapper },
    );

    await act(async () => {
      await result.current.handleSubmit();
    });

    await waitFor(() =>
      expect(result.current.serverErrors).toEqual({
        fullName: "too_short",
        email: "invalid_format",
      }),
    );
    expect(result.current.error).toMatchObject({ code: "validation_failed" });
    expect(onError).toHaveBeenCalledTimes(1);
  });

  it("resets values to defaults and clears dirty state plus server errors", async () => {
    const { wrapper } = makeHarness(async () =>
      jsonResponse(
        {
          code: "validation_failed",
          message: "invalid profile",
          data: { fields: { full_name: "too_short" } },
        },
        400,
      ),
    );

    const { result } = renderHook(
      () =>
        useLazuliForm({
          defaults,
          submit: submitProfile,
          mapValuesToInput: (values) => values,
        }),
      { wrapper },
    );

    act(() => result.current.setValue("fullName", "Ada Lovelace"));
    await act(async () => {
      await result.current.handleSubmit();
    });
    await waitFor(() => expect(result.current.serverErrors.fullName).toBe("too_short"));

    act(() => result.current.reset());

    expect(result.current.values).toEqual(defaults);
    expect(result.current.isDirty).toBe(false);
    expect(result.current.serverErrors).toEqual({});
  });

  it("preserves the generic field shape at compile time", () => {
    function TypeFixture() {
      const form = useLazuliForm({
        defaults,
        submit: submitProfile,
        mapValuesToInput: (values) => values,
      });

      form.setValue("age", 1);
      form.setValues({ fullName: "Ada Lovelace" });
      // @ts-expect-error age requires a number.
      form.setValue("age", "1");
      // @ts-expect-error field must exist on the form values.
      form.setValue("missing", true);

      return form.values.age;
    }

    expect(TypeFixture).toBeTypeOf("function");
  });
});

 succeeded in 1241ms:
// Typed spec objects that generated `dist/web/<feature>/<feature>.gen.ts`
// produces, one per command and query. The runtime client and React hooks
// consume these specs to know the canonical name + which queries to
// invalidate post-mutation, while keeping input/output types attached.
//
// `Input` and `Output` are phantom-typed: they only exist at compile time
// so callers can't pass the wrong shape. At runtime the spec is a plain
// JSON-serialisable record.

// Operational metadata projected onto every CommandSpec / QuerySpec.
// Generated client code populates these fields from the DSL so apps can
// build smart UX (rate-limit-aware backoff, policy-aware button gating,
// audit-aware error reporting) WITHOUT calling a separate metadata RPC.
// The server is still the source of truth — these are hints, not
// enforcement.

export interface PolicySpec {
  /// Canonical policy name as written in the DSL — e.g. `@policy.create`.
  readonly name: string;
  /// Resolved policy atoms — pairs like `{ namespace: "role", name: "admin" }`.
  /// Clients can compare against their session principal to disable
  /// affordances proactively rather than waiting for a 403.
  readonly atoms: readonly PolicyAtom[];
}

export interface PolicyAtom {
  readonly namespace: string;
  readonly name: string;
}

/// Rate-limit envelope. Format mirrors the DSL string ("30 per hour per
/// ip"). Clients can parse for backoff hints; the server enforces.
export type RateLimitSpec = string | null;

/// Audit declaration. `"default"` opts into the framework audit;
/// `"none"` opts out; a string array names specific fields.
export type AuditSpec = "default" | "none" | readonly string[];

export interface CommandSpec<Input, Output> {
  readonly kind: "command";
  readonly name: string;
  readonly invalidates: readonly string[];
  readonly policy?: PolicySpec;
  readonly rateLimit?: RateLimitSpec;
  readonly audit?: AuditSpec;
  // brand keeps the type parameters from being erased to `unknown`.
  readonly _input?: Input;
  readonly _output?: Output;
}

export interface QuerySpec<Args, Result> {
  readonly kind: "query";
  readonly name: string;
  readonly policy?: PolicySpec;
  readonly rateLimit?: RateLimitSpec;
  readonly _args?: Args;
  readonly _result?: Result;
}

export interface DefineCommandOptions {
  readonly invalidates?: readonly string[];
  readonly policy?: PolicySpec;
  readonly rateLimit?: RateLimitSpec;
  readonly audit?: AuditSpec;
}

export interface DefineQueryOptions {
  readonly policy?: PolicySpec;
  readonly rateLimit?: RateLimitSpec;
}

export function defineCommand<Input, Output>(
  name: string,
  options: DefineCommandOptions = {},
): CommandSpec<Input, Output> {
  return {
    kind: "command",
    name,
    invalidates: options.invalidates ?? [],
    // Spread-when-defined keeps `exactOptionalPropertyTypes` happy:
    // omitting an optional key entirely differs from assigning
    // `undefined`. Generated code passes only the fields the author
    // declared in the DSL, so unused metadata stays absent rather
    // than visibly `undefined`.
    ...(options.policy !== undefined ? { policy: options.policy } : {}),
    ...(options.rateLimit !== undefined ? { rateLimit: options.rateLimit } : {}),
    ...(options.audit !== undefined ? { audit: options.audit } : {}),
  };
}

export function defineQuery<Args, Result>(
  name: string,
  options: DefineQueryOptions = {},
): QuerySpec<Args, Result> {
  return {
    kind: "query",
    name,
    ...(options.policy !== undefined ? { policy: options.policy } : {}),
    ...(options.rateLimit !== undefined ? { rateLimit: options.rateLimit } : {}),
  };
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/react.web.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/react.native.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/error.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/exports-parity.test.ts' in c:\tmp\migrate-rhf-adapter
 succeeded in 744ms:
// Concrete WEB entrypoint for `@lazuli/runtime/react`.
//
// Resolved by bundlers/`tsc` via the `default` condition of the
// `./react` exports map. Re-exports every name from `react.ts` (the
// type contract) plus the web bodies of the platform-split hooks. The
// universal hook bodies live here directly because they are pure React
// + TanStack Query — identical across web and native, but each
// entrypoint embeds its own copy to keep the re-export graph local.
//
// See `docs/proposals/mobile-target.md` §3.1 + §3.3.

import {
  useMutation,
  useQuery,
  useQueryClient,
  type UseMutationOptions,
  type UseMutationResult,
  type UseQueryOptions,
  type UseQueryResult,
} from "@tanstack/react-query";
import { createElement, useCallback, useState, type ReactNode } from "react";

import { LazuliClient } from "./client.js";
import type { CommandSpec, QuerySpec } from "./spec.js";
import { LazuliClientContext, useLazuliClient as useLazuliClientImpl } from "./use-actor.js";
import { createUseLazuliAction } from "./use-lazuli-action.js";
import { createUseLazuliForm } from "./use-lazuli-form.js";

// Universal view-helper re-exports — same in `.web.ts` and `.native.ts`.
export {
  canonicalizeSearch,
  parseSegments,
  useFilterState,
  useMultiSelection,
  type DrawerConfig,
  type DrawerSubView,
  type FilterConfig,
  type FilterState,
  type FilterStates,
  type MultiFilterState,
  type MultiSelection,
  type ParsedSegment,
  type SetUrlParams,
  type UrlParams,
} from "./view-helpers.js";

// Platform-split hook bodies (web variants).
export { useLocalSetting } from "./local-setting.web.js";
export { useDrawerSubView } from "./drawer-sub-view.web.js";
export {
  evaluatePolicy,
  withTanStackGuard,
  type LazuliActor,
  type LazuliRouteGuardPolicy,
  type RouteGuardVerdict,
  type WithTanStackGuardOptions,
} from "./route-guard.js";
export {
  RouteGuard,
  useRouteGuardSkeleton,
  withRouteGuard,
  type RouteGuardOptions,
  type RouteGuardProps,
} from "./route-guard-component.js";
export { useActor, useLazuliClient, type UseActorResult } from "./use-actor.js";
export {
  LazuliFlashAdapterContext,
  LazuliFlashAdapterProvider,
  type FlashAdapter,
  type FlashKind,
} from "./flash-adapter.js";
export {
  LazuliRouterAdapterContext,
  LazuliRouterAdapterProvider,
  type RouterAdapter,
} from "./router-adapter.js";
export {
  type UseLazuliActionOptions,
  type UseLazuliActionResult,
} from "./use-lazuli-action.js";
export {
  useLifecycleGate,
  withLifecycleGate,
  type LifecycleGateEvaluator,
  type LifecycleGateMetadata,
  type LifecycleVerdict,
} from "./lifecycle-gate.js";
export type { UseLazuliFormOptions, UseLazuliFormResult } from "./use-lazuli-form.js";
export {
  LazuliRouteParamsError,
  isLazuliRouteParamsError,
  useLazuliRouteParams,
  type RouteParamParser,
  type UseLazuliRouteParamsOptions,
} from "./use-lazuli-route-params.js";

// --- Universal client/query/command hooks --------------------------------

export interface LazuliProviderProps {
  client: LazuliClient;
  children: ReactNode;
}

export function LazuliProvider({ client, children }: LazuliProviderProps) {
  return createElement(LazuliClientContext.Provider, { value: client }, children);
}

export function queryKeyFor(spec: QuerySpec<unknown, unknown>, args: unknown): unknown[] {
  return ["lazuli", spec.name, args];
}

export type UseLazuliQueryOptions<Result> = Omit<
  UseQueryOptions<Result, Error, Result, unknown[]>,
  "queryKey" | "queryFn"
> & {
  client?: LazuliClient;
};

export function useLazuliQuery<Args, Result>(
  spec: QuerySpec<Args, Result>,
  args: Args,
  options: UseLazuliQueryOptions<Result> = {},
): UseQueryResult<Result, Error> {
  const { client: clientOverride, ...queryOptions } = options;
  const client = useLazuliClientImpl(clientOverride);
  return useQuery<Result, Error, Result, unknown[]>({
    queryKey: queryKeyFor(spec, args),
    queryFn: () => client.runQuery(spec, args),
    ...queryOptions,
  });
}

export type UseLazuliCommandOptions<Input, Output> = Omit<
  UseMutationOptions<Output, Error, Input>,
  "mutationFn"
> & {
  client?: LazuliClient;
};

export function useLazuliCommand<Input, Output>(
  spec: CommandSpec<Input, Output>,
  options: UseLazuliCommandOptions<Input, Output> = {},
): UseMutationResult<Output, Error, Input> {
  const { client: clientOverride, onSuccess: userOnSuccess, ...mutationOptions } = options;
  const client = useLazuliClientImpl(clientOverride);
  const queryClient = useQueryClient();
  return useMutation<Output, Error, Input>({
    mutationFn: (input) => client.runCommand(spec, input),
    onSuccess: async (...args) => {
      // Invalidate every TanStack Query cache entry whose key starts with
      // `["lazuli", <invalidated query name>]`. This matches the server's
      // `c.Invalidates` evictions: any args-variant of that query refetches.
      await Promise.all(
        spec.invalidates.map((name) =>
          queryClient.invalidateQueries({ queryKey: ["lazuli", name] }),
        ),
      );
      if (userOnSuccess) {
        await (userOnSuccess as (...a: typeof args) => unknown)(...args);
      }
    },
    ...mutationOptions,
  });
}

export const useLazuliAction = createUseLazuliAction(useLazuliCommand);
export const useLazuliForm = createUseLazuliForm(useLazuliQuery, useLazuliCommand);

// React + react-query primitives re-exported so generated code (cap_file
// hooks, future codegen) can import everything via @lazuli/runtime/react
// without depending on raw `react` / `@tanstack/react-query` modules that
// the dist file's host filesystem location can't always resolve (e.g. a
// pilot's per-app node_modules).
export { useCallback, useState };
export { useQueryClient };

 succeeded in 1243ms:
// Concrete NATIVE entrypoint for `@lazuli/runtime/react`.
//
// Resolved by Metro/`tsc` via the `react-native` condition of the
// `./react` exports map. Re-exports every name from `react.ts` (the
// type contract) plus the native bodies of the platform-split hooks.
// The universal hook bodies are identical across `.web.ts` and
// `.native.ts`; each entrypoint embeds its own copy so the per-target
// resolved module is fully self-contained.
//
// See `docs/proposals/mobile-target.md` §3.1 + §3.3.

import {
  useMutation,
  useQuery,
  useQueryClient,
  type UseMutationOptions,
  type UseMutationResult,
  type UseQueryOptions,
  type UseQueryResult,
} from "@tanstack/react-query";
import { createElement, useCallback, useState, type ReactNode } from "react";

import { LazuliClient } from "./client.js";
import type { CommandSpec, QuerySpec } from "./spec.js";
import { LazuliClientContext, useLazuliClient as useLazuliClientImpl } from "./use-actor.js";
import { createUseLazuliAction } from "./use-lazuli-action.js";
import { createUseLazuliForm } from "./use-lazuli-form.js";

// Universal view-helper re-exports — same in `.web.ts` and `.native.ts`.
export {
  canonicalizeSearch,
  parseSegments,
  useFilterState,
  useMultiSelection,
  type DrawerConfig,
  type DrawerSubView,
  type FilterConfig,
  type FilterState,
  type FilterStates,
  type MultiFilterState,
  type MultiSelection,
  type ParsedSegment,
  type SetUrlParams,
  type UrlParams,
} from "./view-helpers.js";

// Platform-split hook bodies (native variants).
export { useLocalSetting } from "./local-setting.native.js";
export { useDrawerSubView } from "./drawer-sub-view.native.js";
export {
  evaluatePolicy,
  withTanStackGuard,
  type LazuliActor,
  type LazuliRouteGuardPolicy,
  type RouteGuardVerdict,
  type WithTanStackGuardOptions,
} from "./route-guard.js";
export {
  RouteGuard,
  useRouteGuardSkeleton,
  withRouteGuard,
  type RouteGuardOptions,
  type RouteGuardProps,
} from "./route-guard-component.js";
export { useActor, useLazuliClient, type UseActorResult } from "./use-actor.js";
export {
  LazuliFlashAdapterContext,
  LazuliFlashAdapterProvider,
  type FlashAdapter,
  type FlashKind,
} from "./flash-adapter.js";
export {
  LazuliRouterAdapterContext,
  LazuliRouterAdapterProvider,
  type RouterAdapter,
} from "./router-adapter.js";
export {
  type UseLazuliActionOptions,
  type UseLazuliActionResult,
} from "./use-lazuli-action.js";
export {
  useLifecycleGate,
  withLifecycleGate,
  type LifecycleGateEvaluator,
  type LifecycleGateMetadata,
  type LifecycleVerdict,
} from "./lifecycle-gate.js";
export type { UseLazuliFormOptions, UseLazuliFormResult } from "./use-lazuli-form.js";
export {
  LazuliRouteParamsError,
  isLazuliRouteParamsError,
  useLazuliRouteParams,
  type RouteParamParser,
  type UseLazuliRouteParamsOptions,
} from "./use-lazuli-route-params.js";

// --- Universal client/query/command hooks --------------------------------

export interface LazuliProviderProps {
  client: LazuliClient;
  children: ReactNode;
}

export function LazuliProvider({ client, children }: LazuliProviderProps) {
  return createElement(LazuliClientContext.Provider, { value: client }, children);
}

export function queryKeyFor(spec: QuerySpec<unknown, unknown>, args: unknown): unknown[] {
  return ["lazuli", spec.name, args];
}

export type UseLazuliQueryOptions<Result> = Omit<
  UseQueryOptions<Result, Error, Result, unknown[]>,
  "queryKey" | "queryFn"
> & {
  client?: LazuliClient;
};

export function useLazuliQuery<Args, Result>(
  spec: QuerySpec<Args, Result>,
  args: Args,
  options: UseLazuliQueryOptions<Result> = {},
): UseQueryResult<Result, Error> {
  const { client: clientOverride, ...queryOptions } = options;
  const client = useLazuliClientImpl(clientOverride);
  return useQuery<Result, Error, Result, unknown[]>({
    queryKey: queryKeyFor(spec, args),
    queryFn: () => client.runQuery(spec, args),
    ...queryOptions,
  });
}

export type UseLazuliCommandOptions<Input, Output> = Omit<
  UseMutationOptions<Output, Error, Input>,
  "mutationFn"
> & {
  client?: LazuliClient;
};

export function useLazuliCommand<Input, Output>(
  spec: CommandSpec<Input, Output>,
  options: UseLazuliCommandOptions<Input, Output> = {},
): UseMutationResult<Output, Error, Input> {
  const { client: clientOverride, onSuccess: userOnSuccess, ...mutationOptions } = options;
  const client = useLazuliClientImpl(clientOverride);
  const queryClient = useQueryClient();
  return useMutation<Output, Error, Input>({
    mutationFn: (input) => client.runCommand(spec, input),
    onSuccess: async (...args) => {
      // Invalidate every TanStack Query cache entry whose key starts with
      // `["lazuli", <invalidated query name>]`. This matches the server's
      // `c.Invalidates` evictions: any args-variant of that query refetches.
      await Promise.all(
        spec.invalidates.map((name) =>
          queryClient.invalidateQueries({ queryKey: ["lazuli", name] }),
        ),
      );
      if (userOnSuccess) {
        await (userOnSuccess as (...a: typeof args) => unknown)(...args);
      }
    },
    ...mutationOptions,
  });
}

export const useLazuliAction = createUseLazuliAction(useLazuliCommand);
export const useLazuliForm = createUseLazuliForm(useLazuliQuery, useLazuliCommand);

// React + react-query primitives re-exported (parity with react.web.ts) —
// generated code (cap_file hooks) imports via @lazuli/runtime/react.
export { useCallback, useState };
export { useQueryClient };

 succeeded in 1616ms:
// LazuliError mirrors the Go runtime `*lazuli.Error` envelope. The HTTP
// layer encodes failures as JSON:
//
//   { code: "policy_denied", message: "...", data: {...} }
//
// Every typed client method throws an instance of this class on non-2xx
// responses so callers can branch on `err.code` instead of parsing strings.
//
// The canonical code list lives alongside the Go runtime in
// `runtime/go/lazuli/error.go`. We mirror it here as a string-literal union
// to keep autocomplete useful without coupling at compile time.

import type { Json } from "./types.js";

export type LazuliErrorCode =
  | "bad_request"
  | "unauthenticated"
  | "policy_denied"
  | "rate_limited"
  | "validation_failed"
  | "not_found"
  | "tenant_mismatch"
  | "lifecycle_state_mismatch"
  | "conflict"
  | "internal";

export interface LazuliErrorEnvelope {
  code: LazuliErrorCode | string;
  message: string;
  data?: Json | undefined;
  /**
   * Stable identifier for the error message in the app's i18n catalog
   * (e.g. `traveler_authenticated_signin_required`). Emitted by the Go
   * runtime's `writeLazuliError` when the feature contract opts to
   * expose `message_key` (see `i18n.ShouldExpose`). The wire form is
   * snake_case (`message_key`); we surface it camelCased on the
   * runtime instance, matching the rest of `case-mapper`'s convention.
   *
   * Frontend i18n layers should branch on `messageKey` first (most
   * specific) and fall back to `code` when the key is absent or the
   * client catalog doesn't know it. Undefined for legacy callers that
   * predate the Error-Vocab proposal.
   */
  message_key?: string | undefined;
}

export class LazuliError extends Error {
  readonly code: LazuliErrorCode | string;
  readonly status: number;
  readonly data: Json | undefined;
  /** See `LazuliErrorEnvelope.message_key`. */
  readonly messageKey: string | undefined;

  constructor(status: number, envelope: LazuliErrorEnvelope) {
    super(envelope.message);
    this.name = "LazuliError";
    this.code = envelope.code;
    this.status = status;
    this.data = envelope.data;
    this.messageKey = envelope.message_key;
  }
}

export function isLazuliError(err: unknown): err is LazuliError {
  return err instanceof LazuliError;
}

/**
 * Server rejected a command because the resource's lifecycle state
 * doesn't match what the bound transition chain requires. Frontend
 * should refetch the resource and either retry or surface a UX message.
 *
 * Emitted by commands with `triggers transition <...>` declared.
 */
export class LifecycleStateMismatchError extends LazuliError {
  readonly expectedState: string;
  readonly actualState: string;
  readonly transitions: ReadonlyArray<string>;

  constructor(opts: {
    expectedState: string;
    actualState: string;
    transitions: ReadonlyArray<string>;
    message?: string;
  }) {
    super(409, {
      code: "lifecycle_state_mismatch",
      message: opts.message ?? `expected state '${opts.expectedState}', got '${opts.actualState}'`,
      data: {
        expected_state: opts.expectedState,
        actual_state: opts.actualState,
        transitions: [...opts.transitions],
      },
    });
    this.expectedState = opts.expectedState;
    this.actualState = opts.actualState;
    this.transitions = opts.transitions;
  }
}

export function isLifecycleStateMismatchError(err: unknown): err is LifecycleStateMismatchError {
  return err instanceof LifecycleStateMismatchError;
}

 succeeded in 2602ms:
// Exports parity invariant for `@lazuli/runtime/react`.
//
// The type contract (`react.ts`) declares the public surface that both
// concrete entrypoints (`react.web.ts`, `react.native.ts`) must satisfy.
// Type-level satisfaction is enforced by TypeScript at compile time
// (when each `.web`/`.native` re-export bumps into the contract's
// declared signatures). This test guards the runtime-export half:
// every contracted runtime name must be present on the web entrypoint.
// (The native entrypoint is validated by Cell E.2's smoke test, which
// runs with RN installed; Vitest's web jsdom environment can't load
// `react-native` modules.)
//
// See `docs/proposals/mobile-target.md` §3.5 + §11.1 cell A.5.

import { describe, expect, it } from "vitest";

import * as web from "./react.web.js";

/**
 * Every name `@lazuli/runtime/react` is contracted to export. Sourced
 * from `react.ts`. When this list drifts from `react.ts`, the parity
 * test fails — the fix is to update both in lockstep.
 *
 * `export declare ...` in `react.ts` produces zero runtime exports, so
 * we can't introspect `react.ts` directly. The contract is duplicated
 * here as the runtime-side mirror.
 */
const CONTRACTED_RUNTIME_EXPORTS = [
  // Universal client/query/command hooks
  "LazuliProvider",
  "queryKeyFor",
  "useLazuliClient",
  "useLazuliQuery",
  "useLazuliCommand",
  "useLazuliAction",
  "useLazuliForm",
  "useLazuliRouteParams",
  "LazuliRouterAdapterContext",
  "LazuliRouterAdapterProvider",
  "LazuliFlashAdapterContext",
  "LazuliFlashAdapterProvider",
  "LazuliRouteParamsError",
  "isLazuliRouteParamsError",
  "evaluatePolicy",
  "withTanStackGuard",
  "RouteGuard",
  "useRouteGuardSkeleton",
  "withRouteGuard",
  "useActor",
  "useLifecycleGate",
  "withLifecycleGate",
  // Universal view-helper hooks
  "canonicalizeSearch",
  "parseSegments",
  "useFilterState",
  "useMultiSelection",
  // Platform-split hooks
  "useLocalSetting",
  "useDrawerSubView",
  // React + react-query primitive re-exports (for generated cap-file
  // hooks; consumers should still import these from `react` directly).
  "useCallback",
  "useState",
  "useQueryClient",
] as const;

describe("@lazuli/runtime/react — exports parity", () => {
  const webNames = new Set(Object.keys(web));

  it("react.web.ts exports every name declared in react.ts", () => {
    const missing: string[] = [];
    for (const name of CONTRACTED_RUNTIME_EXPORTS) {
      if (!webNames.has(name)) {
        missing.push(name);
      }
    }
    expect(missing).toEqual([]);
  });

  it("react.web.ts does not add un-contracted runtime exports", () => {
    // Additive surface area is a soft violation: anything in `.web.ts`
    // that is not in the contract will work for web consumers but
    // surprise native consumers. Fail loudly.
    const contracted = new Set<string>(CONTRACTED_RUNTIME_EXPORTS);
    const extra: string[] = [];
    for (const name of webNames) {
      if (!contracted.has(name)) {
        extra.push(name);
      }
    }
    expect(extra).toEqual([]);
  });
});

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/tsconfig.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/vitest.config.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/index.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content pnpm-workspace.yaml' in c:\tmp\migrate-rhf-adapter
 succeeded in 289ms:
import { defineConfig } from "vitest/config";

// Minimal vitest setup — happy-dom gives us localStorage, StorageEvent, and
// DOM keydown for the helper tests without spinning a full jsdom. RTL's
// renderHook runs against this same env.
export default defineConfig({
  test: {
    environment: "happy-dom",
    globals: false,
    include: [
      "src/**/*.test.ts",
      "src/**/*.test.tsx",
      "tests/**/*.test.ts",
      "tests/**/*.test.tsx",
    ],
  },
});

 succeeded in 325ms:
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "lib": ["ES2022", "DOM"],
    "jsx": "react-jsx",
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "resolveJsonModule": true,
    "noEmit": true
  },
  "include": ["src/**/*.ts", "src/**/*.tsx"],
  "exclude": ["src/**/*.native.ts", "src/**/*.native.test.ts"]
}

 succeeded in 320ms:
// `@lazuli/runtime` — typed transport for generated `dist/web/<feature>`
// packages. The React entrypoint at `@lazuli/runtime/react` adds TanStack
// Query bindings without forcing React on non-UI consumers (Node scripts,
// tests, edge workers).

export {
  LazuliClient,
  type LazuliClientOptions,
  type LazuliFlash,
  type LazuliRouter,
} from "./client.js";
export {
  LifecycleStateMismatchError,
  LazuliError,
  isLifecycleStateMismatchError,
  isLazuliError,
  type LazuliErrorCode,
  type LazuliErrorEnvelope,
} from "./error.js";
export {
  evaluatePolicy,
  type LazuliActor,
  type LazuliRouteGuardPolicy,
  type RouteGuardVerdict,
} from "./route-guard.js";
export {
  defineCommand,
  defineQuery,
  type AuditSpec,
  type CommandSpec,
  type DefineCommandOptions,
  type DefineQueryOptions,
  type PolicyAtom,
  type PolicySpec,
  type QuerySpec,
  type RateLimitSpec,
} from "./spec.js";
export type { ScalarFixtureProvider, ScalarFixtures } from "./scalars.js";
export {
  formatBrazilianCNPJ,
  formatBrazilianCPF,
  formatBrazilianPhone,
  formatCurrency,
  formatDate,
  formatMoney,
  formatPhone,
  parseBrazilianCNPJInput,
  parseBrazilianCPFInput,
  parseBrazilianPhoneInput,
  parseCurrencyInput,
  parseDateInput,
  parseMoneyInput,
  parsePhoneInput,
} from "./formatters.js";
// `parseId` added by Wave A.7 (useLazuliRouteParams typed parsers).
// c-3 owns `formatMoney` canonically; a-7's types.js variant (if any)
// is shadowed by the formatters.js re-export above. Build will catch
// any name collision.
export { toID, tryID, parseId } from "./types.js";
export type { ID, Json, Money, Time } from "./types.js";
export { messageForLazuliError, type LazuliErrorMessage } from "./message-for-error.js";

 succeeded in 371ms:
packages:
  - "runtime/ts/*"
  - "dist/web/*"

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command Get-ChildItem in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Test-Path pnpm-lock.yaml' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg "react-hook-form|zodResolver|@hookform/resolvers|zod" -n' in c:\tmp\migrate-rhf-adapter
 succeeded in 263ms:
True

 succeeded in 299ms:

    Directory: C:\tmp\migrate-rhf-adapter

[32;1mMode   [0m[32;1m              LastWriteTime[0m [32;1;3m        Length[0m[32;1m Name[0m
[32;1m----   [0m [32;1m             -------------[0m [32;1m        ------[0m [32;1m----[0m
d----          22/05/2026    22:28                [44;1m.vscode[0m
d----          22/05/2026    22:28                [44;1mcrates[0m
d----          22/05/2026    22:28                [44;1mdist[0m
d----          22/05/2026    22:28                [44;1mdocs[0m
d----          22/05/2026    22:28                [44;1meditors[0m
d----          22/05/2026    22:28                [44;1mexamples[0m
d----          22/05/2026    22:28                [44;1mlazurite[0m
d----          22/05/2026    22:28                [44;1mmigrations[0m
d----          22/05/2026    22:28                [44;1mruntime[0m
d----          22/05/2026    22:28                [44;1mscripts[0m
d----          22/05/2026    22:28                [44;1mskills[0m
d----          22/05/2026    22:28                [44;1mtests[0m
d----          22/05/2026    22:28                [44;1mtools[0m
-a---          22/05/2026    22:28            893 .gitignore
-a---          22/05/2026    22:28          11054 AGENTS.md
-a---          22/05/2026    22:28          40112 Cargo.lock
-a---          22/05/2026    22:28            812 Cargo.toml
-a---          22/05/2026    22:28          25260 CHANGELOG.md
-a---          22/05/2026    22:28          11054 CLAUDE.md
-a---          22/05/2026    22:30          79007 CODEX_LOG.txt
-a---          22/05/2026    22:29           4860 CODEX_PROMPT.md
-a---          22/05/2026    22:28           1069 LICENSE
-a---          22/05/2026    22:28            416 package.json
-a---          22/05/2026    22:28         110110 pnpm-lock.yaml
-a---          22/05/2026    22:28             46 pnpm-workspace.yaml
-a---          22/05/2026    22:28           6081 README.md


 succeeded in 2302ms:
CODEX_PROMPT.md:25:import { useForm, type UseFormProps } from "react-hook-form";
CODEX_PROMPT.md:26:import { zodResolver } from "@hookform/resolvers/zod";
CODEX_PROMPT.md:27:import type { ZodSchema } from "zod";
CODEX_PROMPT.md:59:1. Construct `useForm` from RHF with `defaultValues: opts.defaults` and `resolver: opts.schema ? zodResolver(opts.schema) : undefined`.
CODEX_PROMPT.md:79:- **RHF + zod-resolver are peer deps** — add to `runtime/ts/lazuli/package.json` peerDependencies. Apps already have them; runtime doesn't bundle.
CODEX_PROMPT.md:96:Bridges Wave A.3's headless useLazuliForm to apps using react-hook-form
CODEX_PROMPT.md:98:panel migration (each panel: useForm + zodResolver + manual hydrate +
CODEX_PROMPT.md:102:- RHF + zod-resolver as peer deps
CODEX_LOG.txt:38:import { useForm, type UseFormProps } from "react-hook-form";
CODEX_LOG.txt:39:import { zodResolver } from "@hookform/resolvers/zod";
CODEX_LOG.txt:40:import type { ZodSchema } from "zod";
CODEX_LOG.txt:72:1. Construct `useForm` from RHF with `defaultValues: opts.defaults` and `resolver: opts.schema ? zodResolver(opts.schema) : undefined`.
CODEX_LOG.txt:92:- **RHF + zod-resolver are peer deps** — add to `runtime/ts/lazuli/package.json` peerDependencies. Apps already have them; runtime doesn't bundle.
CODEX_LOG.txt:109:Bridges Wave A.3's headless useLazuliForm to apps using react-hook-form
CODEX_LOG.txt:111:panel migration (each panel: useForm + zodResolver + manual hydrate +
CODEX_LOG.txt:115:- RHF + zod-resolver as peer deps
CODEX_LOG.txt:2038:"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg "react-hook-form|zodResolver|@hookform/resolvers|zod" -n' in c:\tmp\migrate-rhf-adapter
crates\lazuli_cli\tests\mobile_smoke.rs:101:            let zod = work.join(format!("dist/ts-mobile/{feature}/{feature}.zod.ts"));
crates\lazuli_cli\tests\mobile_smoke.rs:103:            assert!(zod.exists(), "missing {}", zod.display());
crates\lazuli_cli\tests\mobile_sdk_parity.rs:18://! ("host"), the SDK + zod outputs must match byte-for-byte across
crates\lazuli_cli\tests\mobile_sdk_parity.rs:90:                // Only `.gen.ts` and `.zod.ts` are governed by the
crates\lazuli_cli\tests\mobile_sdk_parity.rs:93:                if !file_name.ends_with(".gen.ts") && !file_name.ends_with(".zod.ts") {
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:71:        write_zod_shim(tempdir.path());
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:114:      "zod": ["./zod-smoke-shim.d.ts"]
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:117:  "include": ["**/*.gen.ts", "**/*.zod.ts"]
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:126:    fn write_zod_shim(out_dir: &Path) {
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:127:        let shim = r#"declare module "zod" {
crates\lazuli_codegen_ts\tests\smoke_ts_typecheck.rs:131:        let path = out_dir.join("zod-smoke-shim.d.ts");
docs\project-structure.md:427:      customer.zod.ts           # zod validation schemas
docs\project-structure.md:433:      customer.zod.ts
crates\lazuli_codegen_ts\src\lib.rs:35:pub mod zod_constraints;
crates\lazuli_codegen_ts\src\lib.rs:42:pub use zod_constraints::{is_numeric, zod_constraint_chain, zod_enum_replacement};
crates\lazuli_codegen_ts\src\lzx.rs:960:        assert!(create_file.contents.contains("zodResolver"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:2://! `zodResolver` pre-bound to the command's input schema.
crates\lazuli_codegen_ts\src\lzx_view_create.rs:42:    // 2. React Hook Form + zodResolver.
crates\lazuli_codegen_ts\src\lzx_view_create.rs:43:    writeln!(s, "import {{ useForm }} from \"react-hook-form\";").ok();
crates\lazuli_codegen_ts\src\lzx_view_create.rs:46:        "import {{ zodResolver }} from \"@hookform/resolvers/zod\";"
crates\lazuli_codegen_ts\src\lzx_view_create.rs:59:        "import {{ {} }} from \"../../{}.zod.js\";",
crates\lazuli_codegen_ts\src\lzx_view_create.rs:151:    writeln!(s, "    resolver: zodResolver({}.schema),", const_name).ok();
crates\lazuli_codegen_ts\src\lzx_view_create.rs:256:        // RHF + zodResolver always wired.
crates\lazuli_codegen_ts\src\lzx_view_create.rs:257:        assert!(out.contains("react-hook-form"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:258:        assert!(out.contains("@hookform/resolvers/zod"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:259:        assert!(out.contains("zodResolver(viewerThingCreateView.schema)"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:281:        assert!(out.contains("import { useForm } from \"react-hook-form\";"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:283:            out.contains("import { zodResolver } from \"@hookform/resolvers/zod\";")
crates\lazuli_codegen_ts\src\lzx_view_create.rs:289:        assert!(out.contains("import { createSlugInputSchema } from \"../../slug.zod.js\";"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:303:        assert!(out.contains("resolver: zodResolver(adminSlugCreateView.schema)"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:358:    fn pre_binds_rhf_and_zod_resolver() {
crates\lazuli_codegen_ts\src\lzx_view_create.rs:359:        // Every create emit must always include the RHF + zodResolver
crates\lazuli_codegen_ts\src\lzx_view_create.rs:369:        assert!(out.contains("zodResolver"));
crates\lazuli_codegen_ts\src\lzx_view_create.rs:370:        assert!(out.contains("resolver: zodResolver"));
crates\lazuli_codegen_ts\src\zod_constraints.rs:53:pub fn zod_constraint_chain(
crates\lazuli_codegen_ts\src\zod_constraints.rs:98:            // `zod_enum_replacement` directly instead.
crates\lazuli_codegen_ts\src\zod_constraints.rs:99:            out.push_str(&format!(".pipe({})", zod_enum_replacement(values)));
crates\lazuli_codegen_ts\src\zod_constraints.rs:120:pub fn zod_enum_replacement(values: &[String]) -> String {
crates\lazuli_codegen_ts\src\zod_constraints.rs:153:        let chain = zod_constraint_chain(&c(), true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:162:        let chain = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:171:        let chain = zod_constraint_chain(&k, false);
crates\lazuli_codegen_ts\src\zod_constraints.rs:179:        let chain = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:187:        let chain = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:195:        let chain = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:203:        let chain = zod_constraint_chain(&k, false);
crates\lazuli_codegen_ts\src\zod_constraints.rs:211:        let chain = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:219:            zod_enum_replacement(&values),
crates\lazuli_codegen_ts\src\zod_constraints.rs:228:        let chain = zod_constraint_chain(&k, false);
crates\lazuli_codegen_ts\src\zod_constraints.rs:239:        let a = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\zod_constraints.rs:240:        let b = zod_constraint_chain(&k, true);
crates\lazuli_codegen_ts\src\runtime.rs:739:/// zod emitter (single source of truth for SDK/zod casing alignment).
crates\lazuli_cli\src\cmd_new_frontends.rs:529:        assert!(pkg.contains("\"react-hook-form\""));
crates\lazuli_cli\src\doctor\schema_rich_001.rs:33:        let generated_files = zod_file_candidates(project_root, &feature.name);
crates\lazuli_cli\src\doctor\schema_rich_001.rs:42:                for slot in crate::command_zod_slots(feature, command, &module) {
crates\lazuli_cli\src\doctor\schema_rich_001.rs:56:                            "generated Zod schema `{schema_ident}` slot `{slot_name}` for `{type_label}` still emits `z.unknown()`; regenerate with rich schema codegen or fix `zod_base_for_type_ref`."
crates\lazuli_cli\src\doctor\schema_rich_001.rs:76:fn zod_file_candidates(project_root: &Path, feature_name: &str) -> Vec<PathBuf> {
crates\lazuli_cli\src\doctor\schema_rich_001.rs:80:        out.push(dir.join(format!("{feature_name}.zod.ts")));
crates\lazuli_cli\src\doctor\schema_rich_001.rs:81:        out.push(dir.join(format!("{feature_name}.zod.gen.ts")));
crates\lazuli_cli\src\doctor\schema_rich_001.rs:185:            "dist/ts-web/catalog/catalog.zod.ts",
crates\lazuli_cli\src\doctor\schema_rich_001.rs:186:            r#"import { z } from "zod";
crates\lazuli_cli\src\doctor\schema_rich_001.rs:222:            "dist/ts-web/catalog/catalog.zod.ts",
crates\lazuli_cli\src\doctor\schema_rich_001.rs:223:            r#"import { z } from "zod";
crates\lazuli_cli\src\main.rs:48://   - `*.gen.go` / `*.gen.ts` / `*.zod.ts` (codegen outputs)
crates\lazuli_cli\src\main.rs:76:dist/**/*.zod.ts
crates\lazuli_cli\src\main.rs:1302:                    "dist/{}/{}/{}.zod.ts",
crates\lazuli_cli\src\main.rs:1305:                contents: emit_feature_zod_ts(feature, module),
crates\lazuli_cli\src\main.rs:2390:fn emit_feature_zod_ts(feature: &lazuli_ir::Feature, module: &lazuli_ir::Module) -> String {
crates\lazuli_cli\src\main.rs:2393:    writeln!(s, "import {{ z }} from \"zod\";").ok();
crates\lazuli_cli\src\main.rs:2402:        for slot in command_zod_slots(feature, command, module) {
crates\lazuli_cli\src\main.rs:2413:                zod_expr_for_slot(&slot.type_ref, &slot.constraints, !slot.required, module)
crates\lazuli_cli\src\main.rs:2450:fn command_zod_slots(
crates\lazuli_cli\src\main.rs:3216:fn zod_expr_for_slot(
crates\lazuli_cli\src\main.rs:3222:    let base = zod_base_for_type_ref(type_ref, module);
crates\lazuli_cli\src\main.rs:3223:    let is_text_base = zod_is_text_base(type_ref);
crates\lazuli_cli\src\main.rs:3227:        lazuli_codegen_ts::zod_constraint_chain(constraints, is_text_base)
crates\lazuli_cli\src\main.rs:3235:fn zod_base_for_type_ref(type_ref: &lazuli_ir::TypeRef, module: &lazuli_ir::Module) -> String {
crates\lazuli_cli\src\main.rs:3249:                zod_base_for_plugin_semantic(name)
crates\lazuli_cli\src\main.rs:3266:            format!("z.array({})", zod_base_for_type_ref(inner, module))
crates\lazuli_cli\src\main.rs:3268:        lazuli_ir::TypeRef::EnumRef(name) => zod_base_for_enum_ref(module, name),
crates\lazuli_cli\src\main.rs:3270:            .map(zod_base_for_enum_decl)
crates\lazuli_cli\src\main.rs:3279:                    return zod_base_for_enum_decl(enum_decl);
crates\lazuli_cli\src\main.rs:3287:fn zod_base_for_enum_ref(
crates\lazuli_cli\src\main.rs:3292:        .map(zod_base_for_enum_decl)
crates\lazuli_cli\src\main.rs:3301:fn zod_base_for_enum_decl(enum_decl: &lazuli_ir::EnumDecl) -> String {
crates\lazuli_cli\src\main.rs:3331:fn zod_base_for_plugin_semantic(name: &str) -> String {
crates\lazuli_cli\src\main.rs:3357:fn zod_is_text_base(type_ref: &lazuli_ir::TypeRef) -> bool {
crates\lazuli_cli\src\main.rs:12492:    fn rich_zod_base_emits_enum_catalog() {
crates\lazuli_cli\src\main.rs:12494:        let schema = crate::zod_base_for_type_ref(
crates\lazuli_cli\src\main.rs:12503:    fn rich_zod_base_emits_core_semantic_validators() {
crates\lazuli_cli\src\main.rs:12526:                crate::zod_base_for_type_ref(&lazuli_ir::TypeRef::Builtin(builtin), &module),
crates\lazuli_cli\src\main.rs:12533:    fn rich_zod_base_emits_plugin_semantic_digit_patterns() {
crates\lazuli_cli\src\main.rs:12555:            crate::zod_base_for_type_ref(&cpf, &module),
crates\lazuli_cli\src\main.rs:12559:            crate::zod_base_for_type_ref(&cnpj, &module),
crates\lazuli_cli\src\main.rs:12563:            crate::zod_base_for_type_ref(&other, &module),
crates\lazuli_cli\src\main.rs:12569:    fn feature_zod_emits_enum_and_semantic_command_schema() {
crates\lazuli_cli\src\main.rs:12588:        let output = crate::emit_feature_zod_ts(&feature, &module);
crates\lazuli_cli\src\main.rs:12592:            "expected enum zod schema, got:\n{output}"
crates\lazuli_cli\src\main.rs:12596:            "expected email zod schema, got:\n{output}"
crates\lazuli_cli\src\templates.rs:325:    "@hookform/resolvers": "^3.9.0",
crates\lazuli_cli\src\templates.rs:337:    "react-hook-form": "^7.52.0",
crates\lazuli_cli\src\templates.rs:341:    "zod": "^3.23.0",

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg "react-hook-form|@hookform/resolvers|''^'"\\s+zod@|/zod@| zod\" pnpm-lock.yaml" in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-ChildItem runtime/ts/lazuli' in c:\tmp\migrate-rhf-adapter
2026-05-23T01:30:23.954507Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 0.3 seconds
Output:

 exited 1 in 279ms:
 succeeded in 308ms:
{
  "name": "lazuli-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Lazuli runtime spike — Go (under runtime/go and dist/go) + TypeScript (under runtime/ts and dist/ts-<frontend>).",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}

 succeeded in 372ms:

    Directory: C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli

[32;1mMode   [0m[32;1m              LastWriteTime[0m [32;1;3m        Length[0m[32;1m Name[0m
[32;1m----   [0m [32;1m             -------------[0m [32;1m        ------[0m [32;1m----[0m
d----          22/05/2026    22:28                [44;1msrc[0m
d----          22/05/2026    22:28                [44;1mtests[0m
-a---          22/05/2026    22:28           1934 package.json
-a---          22/05/2026    22:28            544 tsconfig.json
-a---          22/05/2026    22:28            367 tsconfig.native.json
-a---          22/05/2026    22:28            472 vitest.config.ts


codex
I found the existing headless form hook and its test harness. Before editing, I’m checking the backlog spec section the prompt points at, then I’ll add the isolated entrypoint and tests against the same LazuliProvider/TanStack setup.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "Select-String -Path C:\\Users\\lucas\\lazuli-ops\\docs\\proposals\\hostpoint-migration-backlog-2026-05-22.md -Pattern \"M\\.2\" -Context 6,80" in c:\tmp\migrate-rhf-adapter
 succeeded in 310ms:

  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:1:---
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:2:name: 
hostpoint-migration-backlog
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:3:description: |
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:4:  Hostpoint pilot migration 
backlog post Rails-leap substrate ship.
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:5:  Substrate (lazuli IR + 
codegen + runtime) is on main; this doc tracks
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:6:  the per-domain consumption 
waves M.1-M.7 introduced 2026-05-22. M.1
> C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:7:  shipped; [7mM.2[0m/M.3/M.4 [0m
[7m[0mevaluated [0m[7m[0mand [0m[7m[0meither [0m[7m[0mdeferred [0m[7m[0mor [0m[7m[0mmarked [0m[7m[0mno-op[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:8: [0m[7m [0m[7m[0mwith [0m[7m[0mrationale; [0m[7m[0mM.5/M.6/M.7 [0m
[7m[0mare [0m[7m[0mnext-session [0m[7m[0mscope [0m[7m[0mwith [0m[7m[0mexplicit[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:9:  acceptance criteria.[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:10:status: [0m[7m[0mactive [0m[7m[0mbacklog [0m[7m[0m— [0m
[7m[0msession [0m[7m[0m2026-05-22 [0m[7m[0mclosed [0m[7m[0mwith [0m[7m[0mM.1 [0m[7m[0mshipped[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:11:audience: [0m[7m[0mhostpoint [0m[7m[0mpilot [0m
[7m[0mteam, [0m[7m[0mfuture [0m[7m[0mmigration [0m[7m[0msessions[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:12:date: 2026-05-22[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:13:parent_plan: [0m
[7m[0mdocs/proposals/lazuli-rails-leap-master-plan-2026-05-22.md[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:14:---[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:15:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:16:# Hostpoint Migration Backlog[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:17:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:18:## Session 2026-05-22 outcome[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:19:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:20:Substrate [0m[7m[0mcomplete [0m[7m[0m(17 [0m
[7m[0mrails-leap [0m[7m[0mbranches [0m[7m[0mmerged, [0m[7m[0mruntime [0m[7m[0m+ [0m[7m[0mcodegen [0m[7m[0m+[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:21:doctor [0m[7m[0mshipped). [0m[7m[0mPilot [0m
[7m[0mconsumption [0m[7m[0mmigration [0m[7m[0mstarted:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:22:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:23:| Wave | Status | Notes |[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:24:|---|---|---|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:25:| [0m[7m[0m**M.1** [0m[7m[0mavatar [0m[7m[0mupload [0m[7m[0m| [0m[7m[0m✅ [0m
[7m[0mshipped [0m[7m[0m| [0m[7m[0mHostAccountHome [0m[7m[0m+ [0m[7m[0mTravelerHome [0m[7m[0mnow [0m[7m[0mconsume [0m[7m[0m`useHost/TravelerProfilePhotoUpload` [0m[7m[0mfrom [0m
[7m[0m`dist/ts-web/<feature>/<feature>.cap-file.react.gen.ts`. [0m[7m[0mDeleted [0m[7m[0m~150 [0m[7m[0mLoC [0m[7m[0mof [0m[7m[0mhand-rolled [0m[7m[0m`use-avatar-upload.ts`. [0m
[7m[0mRequired [0m[7m[0mcodegen [0m[7m[0mfix: [0m[7m[0mcap-file [0m[7m[0mhooks [0m[7m[0mnow [0m[7m[0mimport [0m[7m[0mvia [0m[7m[0m`@lazuli/runtime/react` [0m[7m[0mre-exports [0m[7m[0m(commit [0m[7m[0m`9dea6e7`) [0m[7m[0m— [0m[7m[0mpilots' [0m
[7m[0mdist [0m[7m[0mlocations [0m[7m[0mcan't [0m[7m[0mresolve [0m[7m[0mraw [0m[7m[0m`react` [0m[7m[0mvia [0m[7m[0mfilesystem [0m[7m[0mwalk. [0m[7m[0mHostpoint [0m[7m[0mcommit: [0m[7m[0m`2f44b30` [0m[7m[0m(-213 [0m[7m[0mLoC, [0m[7m[0m+17 [0m[7m[0mLoC). [0m[7m[0m|[0m
> C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:26:| **[7mM.2[0m** [0m[7m[0msettings [0m[7m[0mpanels [0m[7m[0m| [0m[7m[0m⏸ [0m
[7m[0mdeferred [0m[7m[0m| [0m[7m[0mExisting [0m[7m[0mpanels [0m[7m[0mbind [0m[7m[0mRHF [0m[7m[0m`<Controller>` [0m[7m[0mto [0m[7m[0m`<Input>`/`<Select>`/`<DatePicker>` [0m[7m[0mUI [0m[7m[0mprimitives. [0m
[7m[0m`useLazuliForm` [0m[7m[0m(Wave [0m[7m[0mA.3) [0m[7m[0mis [0m[7m[0mRHF-agnostic [0m[7m[0m— [0m[7m[0musing [0m[7m[0mit [0m[7m[0mwould [0m[7m[0mrequire [0m[7m[0mrewriting [0m[7m[0mevery [0m[7m[0mUI [0m[7m[0minput [0m[7m[0mbinding [0m[7m[0m(much [0m[7m[0mhigher [0m
[7m[0mchurn [0m[7m[0mthan [0m[7m[0mthe [0m[7m[0msavings). [0m[7m[0m**Prereq**: [0m[7m[0mship [0m[7m[0m`useLazuliForm [0m[7m[0m+ [0m[7m[0mRHF [0m[7m[0madapter` [0m[7m[0m(`@lazuli/runtime/react/rhf` [0m[7m[0msubpath) [0m[7m[0mthat [0m
[7m[0mexposes [0m[7m[0m`register`/`Controller`-compatible [0m[7m[0mbindings. [0m[7m[0mEstimated [0m[7m[0meffort: [0m[7m[0m1 [0m[7m[0msession [0m[7m[0mfor [0m[7m[0mthe [0m[7m[0madapter [0m[7m[0m+ [0m[7m[0m1 [0m[7m[0msession [0m[7m[0mfor [0m[7m[0mthe [0m
[7m[0m14-panel [0m[7m[0mmigration. [0m[7m[0m|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:27:| [0m[7m[0m**M.3** [0m[7m[0mlist [0m[7m[0mscreens [0m[7m[0muse [0m
[7m[0mgenerated [0m[7m[0mhooks [0m[7m[0m| [0m[7m[0m⏸ [0m[7m[0mdeferred [0m[7m[0m| [0m[7m[0mPure [0m[7m[0mmechanical [0m[7m[0mswap [0m[7m[0mof [0m[7m[0m`useLazuliQuery(lookupMyHost, [0m[7m[0m{})` [0m[7m[0m→ [0m[7m[0m`useLookupMyHost()` [0m[7m[0macross [0m
[7m[0m~25 [0m[7m[0mcallsites. [0m[7m[0mLow [0m[7m[0mvalue [0m[7m[0m(8 [0m[7m[0mchars [0m[7m[0msaved [0m[7m[0mper [0m[7m[0mcall, [0m[7m[0mno [0m[7m[0mLoC [0m[7m[0mreduction, [0m[7m[0mno [0m[7m[0msemantic [0m[7m[0mchange). [0m[7m[0mCan [0m[7m[0mbe [0m[7m[0mdone [0m[7m[0mlazily [0m[7m[0mas [0m
[7m[0mtouched, [0m[7m[0mor [0m[7m[0mbatched. [0m[7m[0mDefer [0m[7m[0munless [0m[7m[0mcross-cutting [0m[7m[0mrefactor [0m[7m[0mjustifies. [0m[7m[0m|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:28:| [0m[7m[0m**M.4** [0m[7m[0mformatLazuliError [0m
[7m[0mwrapper [0m[7m[0m| [0m[7m[0m❌ [0m[7m[0mno-op [0m[7m[0m| [0m[7m[0mThe [0m[7m[0mpilot's [0m[7m[0m`formatLazuliError` [0m[7m[0mdoes [0m[7m[0mmeaningful [0m[7m[0mcatalog-mapping [0m[7m[0mwork [0m[7m[0m(codes [0m[7m[0m→ [0m[7m[0m`errors.<x>` [0m[7m[0mkeys, [0m
[7m[0mparam [0m[7m[0minterpolation, [0m[7m[0mnetwork [0m[7m[0mdetection, [0m[7m[0m5xx [0m[7m[0mfallback). [0m[7m[0mThe [0m[7m[0mruntime's [0m[7m[0m`messageForLazuliError` [0m[7m[0monly [0m[7m[0mreturns [0m[7m[0ma [0m[7m[0mthin [0m
[7m[0mdescriptor [0m[7m[0m`{key, [0m[7m[0mfallback, [0m[7m[0mparams, [0m[7m[0mcode}` [0m[7m[0m— [0m[7m[0ma [0m[7m[0mwrapper [0m[7m[0mover [0m[7m[0mit [0m[7m[0mwould [0m[7m[0mhave [0m[7m[0mto [0m[7m[0mre-add [0m[7m[0mall [0m[7m[0mthe [0m[7m[0mcatalog [0m[7m[0mmapping. [0m[7m[0mKept [0m
[7m[0m`formatLazuliError` [0m[7m[0mas [0m[7m[0mthe [0m[7m[0mcanonical [0m[7m[0mapp-side [0m[7m[0mmapper. [0m[7m[0mThe [0m[7m[0mruntime [0m[7m[0mhelper [0m[7m[0mstays [0m[7m[0mavailable [0m[7m[0mfor [0m[7m[0mapps [0m[7m[0mthat [0m[7m[0mDO [0m[7m[0mwant [0m[7m[0mthe [0m
[7m[0mthin [0m[7m[0mdescriptor [0m[7m[0m(third-party [0m[7m[0mSDK [0m[7m[0mconsumers). [0m[7m[0m|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:29:| [0m[7m[0m**M.5** [0m[7m[0mroute [0m[7m[0mtable [0m[7m[0min [0m
[7m[0m`.lzx` [0m[7m[0m| [0m[7m[0m🔜 [0m[7m[0mnext [0m[7m[0msession [0m[7m[0m| [0m[7m[0mDetailed [0m[7m[0mbelow. [0m[7m[0m|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:30:| [0m[7m[0m**M.6** [0m[7m[0mApp.tsx [0m[7m[0m→ [0m
[7m[0mproviders-only [0m[7m[0m| [0m[7m[0m🔜 [0m[7m[0mfollows [0m[7m[0mM.5 [0m[7m[0m| [0m[7m[0mDetailed [0m[7m[0mbelow. [0m[7m[0m|[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:31:| [0m[7m[0m**M.7** [0m[7m[0mhostpoint-os [0m
[7m[0mmigration [0m[7m[0m| [0m[7m[0m🔜 [0m[7m[0mfollows [0m[7m[0mM.6 [0m[7m[0m| [0m[7m[0mSmaller [0m[7m[0mscope [0m[7m[0m(operator [0m[7m[0mdashboard); [0m[7m[0mapply [0m[7m[0msame [0m[7m[0mpattern. [0m[7m[0m|[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:32:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:33:## [0m[7m[0mNet [0m[7m[0mLoC [0m[7m[0mchange [0m[7m[0msession [0m
[7m[0m2026-05-22[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:34:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:35:- [0m[7m[0mHostpoint: [0m[7m[0m−196 [0m[7m[0mLoC [0m[7m[0m(M.1 [0m
[7m[0mavatar [0m[7m[0mupload [0m[7m[0mdelete [0m[7m[0m+ [0m[7m[0mthin [0m[7m[0mre-integration)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:36:- [0m[7m[0mLazuli: [0m[7m[0m+52 [0m[7m[0mLoC [0m[7m[0mruntime [0m
[7m[0mre-exports [0m[7m[0m+ [0m[7m[0m1-line [0m[7m[0memitter [0m[7m[0mfix [0m[7m[0m(the [0m[7m[0mcodegen [0m[7m[0mdrift [0m[7m[0munblocking [0m[7m[0mM.1)[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:37:[0m
> C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:38:## [7mM.2 [0m[7m[0munblock [0m[7m[0mspec [0m[7m[0m— [0m
[7m[0m`useLazuliForm` [0m[7m[0m+ [0m[7m[0mRHF [0m[7m[0madapter[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:39:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:40:**What**: [0m[7m[0mNew [0m
[7m[0m`@lazuli/runtime/react/rhf` [0m[7m[0msubpath [0m[7m[0mthat [0m[7m[0mexposes [0m[7m[0ma[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:41:`useLazuliForm`-backed [0m[7m[0mform [0m
[7m[0mwhere:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:42:- [0m[7m[0mForm [0m[7m[0mvalues [0m[7m[0m+ [0m[7m[0mdirty [0m[7m[0mstate [0m[7m[0m+ [0m
[7m[0msubmit [0m[7m[0m+ [0m[7m[0mserver [0m[7m[0mvalidation [0m[7m[0mcome [0m[7m[0mfrom [0m[7m[0m`useLazuliForm`.[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:43:- [0m[7m[0mField [0m[7m[0mbindings [0m[7m[0mgo [0m[7m[0mthrough [0m
[7m[0mRHF's [0m[7m[0m`useController` [0m[7m[0mfor [0m[7m[0mcompat [0m[7m[0mwith [0m[7m[0mexisting [0m[7m[0mUI [0m[7m[0mprimitives [0m[7m[0m(`<Input>`, [0m[7m[0m`<Select>`, [0m[7m[0m`<DatePicker>` [0m[7m[0mconsume [0m
[7m[0m`field.value`, [0m[7m[0m`field.onChange`, [0m[7m[0m`fieldState.error`).[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:44:- [0m[7m[0mSchema [0m[7m[0mintegration [0m[7m[0mvia [0m
[7m[0mexisting [0m[7m[0m`dist/ts-web/<feature>/<feature>.zod.ts` [0m[7m[0m(Wave [0m[7m[0mA.4 [0m[7m[0memits [0m[7m[0mrich [0m[7m[0mZod [0m[7m[0m— [0m[7m[0malready [0m[7m[0mshipped).[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:45:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:46:**Where**:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:47:- [0m[7m[0mNew [0m[7m[0mfile: [0m
[7m[0m`runtime/ts/lazuli/src/react/rhf/use-lazuli-form-rhf.ts`[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:48:- [0m[7m[0mNew [0m[7m[0mpackage.json [0m[7m[0msubpath: [0m
[7m[0m`@lazuli/runtime/react/rhf`[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:49:- [0m[7m[0mTests: [0m[7m[0mcovering [0m
[7m[0mhydrate-on-mount, [0m[7m[0msubmit, [0m[7m[0mserver [0m[7m[0mvalidation [0m[7m[0mpropagation [0m[7m[0mto [0m[7m[0mRHF [0m[7m[0merrors, [0m[7m[0mdirty [0m[7m[0mstate.[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:50:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:51:**Acceptance**: [0m[7m[0ma [0m[7m[0mhostpoint [0m
[7m[0msettings [0m[7m[0mpanel [0m[7m[0m(e.g. [0m[7m[0m`HostPersonal.tsx`)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:52:migrates [0m[7m[0mfrom [0m
[7m[0m`useForm(zodResolver(schema))` [0m[7m[0m+ [0m[7m[0mmanual [0m[7m[0mhydrate [0m[7m[0m+ [0m[7m[0mmanual[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:53:save [0m[7m[0m+ [0m[7m[0mmanual [0m[7m[0merror [0m[7m[0mto [0m
[7m[0m`useLazuliFormRHF(lookupMyHost, [0m[7m[0mupdateHost,[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:54:schema)` [0m[7m[0mwith [0m[7m[0mthe [0m[7m[0msame [0m[7m[0mUI [0m
[7m[0mbindings. [0m[7m[0mGoal: [0m[7m[0m~50% [0m[7m[0mLoC [0m[7m[0mreduction [0m[7m[0mper[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:55:panel (149 LoC → ~75 LoC).[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:56:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:57:## [0m[7m[0mM.5 [0m[7m[0m+ [0m[7m[0mM.6 [0m[7m[0mspec [0m[7m[0m— [0m[7m[0mroute [0m
[7m[0mtable [0m[7m[0mmigration[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:58:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:59:### Scope[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:60:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:61:57 [0m[7m[0mroutes [0m[7m[0min [0m
[7m[0m`app/clients/hostpoint-app/src/shell/App.tsx`:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:62:- [0m[7m[0m8 [0m[7m[0mpublic [0m[7m[0mroutes [0m[7m[0m(`/`, [0m
[7m[0m`/welcome`, [0m[7m[0m`/sign-in`, [0m[7m[0m`/sign-up`, [0m[7m[0m`/choose-role`, [0m[7m[0m`/forgot-password*`, [0m[7m[0m`/forbidden`)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:63:- [0m[7m[0m5 [0m[7m[0mhost [0m[7m[0monboarding [0m[7m[0mroutes [0m
[7m[0m(`/onboarding/host/*`)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:64:- [0m[7m[0m7 [0m[7m[0mtraveler [0m[7m[0monboarding [0m[7m[0mroutes [0m
[7m[0m(`/onboarding/traveler/*`)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:65:- [0m[7m[0m2 [0m[7m[0maccount-shared [0m[7m[0mroutes [0m
[7m[0m(`/account/security`, [0m[7m[0m`/legal/*`)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:66:- [0m[7m[0m~35 [0m[7m[0mauthenticated [0m
[7m[0mhost/traveler/account [0m[7m[0mroutes[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:67:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:68:### [0m[7m[0mM.5.1 [0m[7m[0m— [0m[7m[0mauthor [0m
[7m[0m`app/hostpoint.lzx` [0m[7m[0m(new [0m[7m[0mfile)[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:69:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:70:Top-level [0m[7m[0m`.lzx` [0m[7m[0mdeclaring [0m
[7m[0mroute [0m[7m[0mblocks [0m[7m[0m1:1 [0m[7m[0mwith [0m[7m[0m`App.tsx` [0m[7m[0mpaths.[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:71:Each route has:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:72:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:73:```lzx[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:74:route host_home[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:75:  path "/host"[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:76:  to host.view.host_home[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:77:  surface host web[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:78:  audience host[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:79:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:80:route host_onboarding_terms[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:81: [0m[7m [0m[7m[0mpath [0m
[7m[0m"/onboarding/host/intermediation"[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:82: [0m[7m [0m[7m[0mto [0m
[7m[0mhost.view.intermediation_terms[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:83:  surface host web[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:84:  audience host[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:85:```[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:86:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:87:### [0m[7m[0mM.5.2 [0m[7m[0m— [0m[7m[0mview [0m[7m[0mblocks [0m[7m[0mper [0m
[7m[0mfeature[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:88:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:89:For [0m[7m[0meach [0m[7m[0mroute, [0m[7m[0mthe [0m[7m[0mtarget [0m
[7m[0mfeature [0m[7m[0mgets [0m[7m[0ma [0m[7m[0m`view` [0m[7m[0mblock [0m[7m[0min [0m[7m[0mits [0m[7m[0m`.lzx`:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:90:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:91:```lzx[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:92:# [0m
[7m[0mapp/features/host/host.web.lzx[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:93:experience host[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:94:  imports host[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:95:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:96:  view host_home[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:97:    policy @policy.host_only[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:98: [0m[7m [0m[7m [0m[7m [0m[7m[0mrequires_lifecycle [0m[7m[0mHost [0m[7m[0m= [0m
[7m[0mcomplete[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:99: [0m[7m [0m[7m [0m[7m [0m[7m[0mon_lifecycle_pending [0m
[7m[0m@resume [0m[7m[0mhost_onboarding[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:100: [0m[7m [0m[7m [0m[7m [0m[7m[0msubmit [0m
[7m[0mhost.command.get_host_home[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:101:```[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:102:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:103:Resume routers per role:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:104:[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:105:```lzx[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:106:resume host_onboarding[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:107:  source query.lookup_my_host[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:108: [0m[7m [0m[7m[0mnone [0m[7m[0m→ [0m[7m[0mview [0m
[7m[0mintermediation_terms[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:109: [0m[7m [0m
[7m[0mintermediation_terms_pending [0m[7m[0m→ [0m[7m[0mview [0m[7m[0mintermediation_terms[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:110: [0m[7m [0m[7m[0mbasic_details_pending [0m[7m[0m→ [0m
[7m[0mview [0m[7m[0mbasic_details[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:111: [0m[7m [0m[7m[0maddress_pending [0m[7m[0m→ [0m[7m[0mview [0m
[7m[0maddress[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:112: [0m[7m [0m[7m[0mlanguages_pending [0m[7m[0m→ [0m[7m[0mview [0m
[7m[0mlanguages[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:113:  complete → view host_home[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:114:```[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:115:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:116:### [0m[7m[0mM.5.3 [0m[7m[0m— [0m
[7m[0m`when_denied_route` [0m[7m[0mon [0m[7m[0mpolicies[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:117:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:118:`app/features/account/account.[0m
[7m[0mlzi` [0m[7m[0m(or [0m[7m[0mper-feature) [0m[7m[0mextends [0m[7m[0mexisting[0m
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:240:- **A.2.2 `{result.X}` 
interpolation** — shipped (`a3686cf`); add cross-domain test once a `.lzx` view uses `on_success / redirect 
"/x/{result.id}"` end-to-end.
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:241:- **Naming pluralization in 
pilot consumers** — A.6 ships deprecation aliases for one release window. After M.3 lands (mechanical hook swap), 
confirm zero references to deprecated names; THEN remove aliases from codegen.
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:242:
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:243:## Next-session entry point
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:244:
  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:245:Recommended order:
> C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:246:1. **useLazuliForm + RHF 
adapter** (unblocks [7mM.2[0m)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:247:2. [0m[7m[0m**M.5 [0m[7m[0m+ [0m[7m[0mM.6** [0m[7m[0m(heaviest, [0m
[7m[0mhighest [0m[7m[0mvalue)[0m
> C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:248:3. **[7mM.2 [0m[7m[0mpanel [0m[7m[0mmigration** [0m
[7m[0m(after [0m[7m[0madapter)[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:249:4. [0m[7m[0m**M.3** [0m[7m[0mmechanical [0m[7m[0msweep [0m
[7m[0m(cleanup)[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:250:5. **M.7** OS migration[0m
[7m[0m  C:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:251:[0m
[7m [0m[7m [0m[7m[0mC:\Users\lucas\lazuli-ops\docs\proposals\hostpoint-migration-backlog-2026-05-22.md:252:Total [0m[7m[0mestimated [0m[7m[0mcompletion: [0m
[7m[0m3-4 [0m[7m[0mdedicated [0m[7m[0msessions [0m[7m[0m× [0m[7m[0m~4h [0m[7m[0meach.[0m


exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/src/local-setting.web.ts' in c:\tmp\migrate-rhf-adapter
 succeeded in 1074ms:
// Web body for `useLocalSetting` — backed by `localStorage` and synced
// across tabs via the `storage` event. The hook returns the persisted
// value synchronously on first render (uses `useSyncExternalStore`).
//
// See `docs/proposals/mobile-target.md` §4.1 for the per-platform
// contract; the native counterpart is `local-setting.native.ts`. Both
// implementations share the JSDoc-documented divergence on first-render
// behavior (web: sync; native: async).

import { useCallback, useRef, useSyncExternalStore } from "react";

function subscribeLocal(key: string, onChange: () => void): () => void {
  if (typeof window === "undefined") return () => {};
  const handler = (e: StorageEvent) => {
    if (e.key === key || e.key === null) onChange();
  };
  window.addEventListener("storage", handler);
  return () => window.removeEventListener("storage", handler);
}

/**
 * `useState`-shaped tuple backed by `localStorage`. Listens for `storage`
 * events so two tabs editing the same key converge. JSON-serialized.
 *
 * IMPORTANT — first-render contract differs by platform:
 *   - Web (this file): returns the persisted value synchronously on
 *     first render (uses useSyncExternalStore + localStorage).
 *   - Native (`local-setting.native.ts`): returns `defaultValue` on
 *     first render; the persisted value becomes visible on the next
 *     render after AsyncStorage resolves.
 *
 * Callers MUST treat the value as eventually-consistent. Code that
 * depends on the persisted value being present on the first frame
 * (e.g., to pick a theme before paint) must read storage explicitly
 * via the platform's lower-level API, not via this hook.
 */
export function useLocalSetting<T>(key: string, defaultValue: T): [T, (next: T) => void] {
  // Use a ref to bridge useSyncExternalStore (which expects a snapshot
  // identity) and JSON.parse (which always produces fresh objects).
  const cacheRef = useRef<{ raw: string | null; value: T }>({ raw: null, value: defaultValue });

  const getSnapshot = useCallback((): T => {
    if (typeof window === "undefined" || !window.localStorage) return defaultValue;
    const raw = window.localStorage.getItem(key);
    if (raw === cacheRef.current.raw) return cacheRef.current.value;
    try {
      const parsed = raw === null ? defaultValue : (JSON.parse(raw) as T);
      cacheRef.current = { raw, value: parsed };
      return parsed;
    } catch {
      cacheRef.current = { raw, value: defaultValue };
      return defaultValue;
    }
  }, [key, defaultValue]);

  const subscribe = useCallback((onChange: () => void) => subscribeLocal(key, onChange), [key]);

  const getServerSnapshot = useCallback((): T => defaultValue, [defaultValue]);

  const value = useSyncExternalStore(subscribe, getSnapshot, getServerSnapshot);

  const setValue = useCallback(
    (next: T) => {
      if (typeof window === "undefined" || !window.localStorage) return;
      const serialized = JSON.stringify(next);
      window.localStorage.setItem(key, serialized);
      cacheRef.current = { raw: serialized, value: next };
      // Fire a synthetic storage event so the same-tab subscriber re-reads.
      window.dispatchEvent(new StorageEvent("storage", { key, newValue: serialized }));
    },
    [key],
  );

  return [value, setValue];
}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in c:\tmp\migrate-rhf-adapter
 succeeded in 274ms:
?? CODEX_LOG.txt
?? CODEX_PROMPT.md

codex
I’m going to add the RHF subpath as a separate entry file, keep the core `@lazuli/runtime/react` export unchanged, and return the normal RHF form methods plus the Lazuli-wired `onSubmit`/`submitError`/partial `reset` surface.
apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react-rhf.ts
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\package.json
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..11d4320cdc36e5f30777e88712ddf00ebf508414
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
+    "react-hook-form": ">=7.52.0",
     "react-native": ">=0.74.0"
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..11d4320cdc36e5f30777e88712ddf00ebf508414
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
+    "react-hook-form": ">=7.52.0",
     "react-native": ">=0.74.0"
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
 succeeded in 419ms:
{
  "name": "@lazuli/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Lazuli universal runtime — typed client + React hooks (TanStack Query). One package serves web and React Native via the exports map's `react-native` condition; see docs/proposals/mobile-target.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./react": {
      "types": "./src/react.ts",
      "react-native": "./src/react.native.ts",
      "default": "./src/react.web.ts"
    },
    "./react/tanstack": {
      "types": "./src/tanstack-adapter.ts",
      "import": "./src/tanstack-adapter.ts",
      "default": "./src/tanstack-adapter.ts"
    },
    "./react/rhf": {
      "types": "./src/react-rhf.ts",
      "import": "./src/react-rhf.ts",
      "default": "./src/react-rhf.ts"
    },
    "./formatters": {
      "types": "./src/formatters.ts",
      "import": "./src/formatters.ts",
      "default": "./src/formatters.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "tsc": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "search-query-parser": "^1.6.0"
  },
  "peerDependencies": {
    "@hookform/resolvers": ">=3.9.0",
    "@react-native-async-storage/async-storage": ">=1.23.0",
    "@tanstack/react-query": ">=5.0.0",
    "@tanstack/react-router": ">=1.0.0",
    "react": ">=18.0.0",
    "react-hook-form": ">=7.52.0",
    "react-native": ">=0.74.0"
    "zod": ">=3.23.0"
  },
  "peerDependenciesMeta": {
    "@hookform/resolvers": { "optional": true },
    "@react-native-async-storage/async-storage": { "optional": true },
    "@tanstack/react-query": { "optional": true },
    "@tanstack/react-router": { "optional": true },
    "react": { "optional": true },
    "react-hook-form": { "optional": true },
    "react-native": { "optional": true },
    "zod": { "optional": true }
  },
  "devDependencies": {
    "@hookform/resolvers": "^3.9.0",
    "@tanstack/react-query": "^5.59.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "happy-dom": "^15.11.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-hook-form": "^7.52.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "zod": "^3.23.0"
  }
}

diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..11d4320cdc36e5f30777e88712ddf00ebf508414
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
+    "react-hook-form": ">=7.52.0",
     "react-native": ">=0.74.0"
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\package.json
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react-rhf.test.tsx
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\STATUS.md
diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
The implementation, tests, export map, and root `STATUS.md` sample are in place. I’m installing the new peer-backed dev dependencies now so the lockfile and package tests line up.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm install' in c:\tmp\migrate-rhf-adapter
 succeeded in 10127ms:
Scope: all 4 workspace projects
Progress: resolved 0, reused 1, downloaded 0, added 0
Progress: resolved 378, reused 305, downloaded 0, added 0
Packages: +308
++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++++
Progress: resolved 380, reused 307, downloaded 1, added 13
Progress: resolved 380, reused 307, downloaded 1, added 102
Progress: resolved 380, reused 307, downloaded 1, added 174
Progress: resolved 380, reused 307, downloaded 1, added 207
Progress: resolved 380, reused 307, downloaded 1, added 279
Progress: resolved 380, reused 307, downloaded 1, added 306
Progress: resolved 380, reused 307, downloaded 1, added 307
Progress: resolved 380, reused 307, downloaded 1, added 308, done
 WARN  Issues with peer dependencies found
runtime/ts/lazuli
└─┬ react-native 0.85.3
  ├── ✕ unmet peer @types/react@^19.1.1: found 18.3.28
  ├── ✕ unmet peer react@^19.2.3: found 18.3.1
  └─┬ @react-native/virtualized-lists 0.85.3
    └── ✕ unmet peer @types/react@^19.2.0: found 18.3.28

devDependencies:
+ tsx 4.21.0
+ typescript 5.9.3

╭ Warning ─────────────────────────────────────────────────────────────────────╮
│                                                                              │
│   Ignored build scripts: esbuild@0.21.5, esbuild@0.27.7.                     │
│   Run "pnpm approve-builds" to pick which dependencies should be allowed     │
│   to run scripts.                                                            │
│                                                                              │
╰──────────────────────────────────────────────────────────────────────────────╯
Done in 9.7s using pnpm v10.32.1

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Get-Content package.json' in c:\tmp\migrate-rhf-adapter
 succeeded in 255ms:
 M pnpm-lock.yaml
 M runtime/ts/lazuli/package.json
?? CODEX_LOG.txt
?? CODEX_PROMPT.md
?? STATUS.md
?? runtime/ts/lazuli/src/react-rhf.test.tsx
?? runtime/ts/lazuli/src/react-rhf.ts

 succeeded in 280ms:
{
  "name": "@lazuli/runtime",
  "version": "0.0.0",
  "private": true,
  "description": "Lazuli universal runtime — typed client + React hooks (TanStack Query). One package serves web and React Native via the exports map's `react-native` condition; see docs/proposals/mobile-target.md §3.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    },
    "./react": {
      "types": "./src/react.ts",
      "react-native": "./src/react.native.ts",
      "default": "./src/react.web.ts"
    },
    "./react/tanstack": {
      "types": "./src/tanstack-adapter.ts",
      "import": "./src/tanstack-adapter.ts",
      "default": "./src/tanstack-adapter.ts"
    },
    "./react/rhf": {
      "types": "./src/react-rhf.ts",
      "import": "./src/react-rhf.ts",
      "default": "./src/react-rhf.ts"
    },
    "./formatters": {
      "types": "./src/formatters.ts",
      "import": "./src/formatters.ts",
      "default": "./src/formatters.ts"
    }
  },
  "scripts": {
    "build": "tsc --noEmit",
    "tsc": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "search-query-parser": "^1.6.0"
  },
  "peerDependencies": {
    "@hookform/resolvers": ">=3.9.0",
    "@react-native-async-storage/async-storage": ">=1.23.0",
    "@tanstack/react-query": ">=5.0.0",
    "@tanstack/react-router": ">=1.0.0",
    "react": ">=18.0.0",
    "react-hook-form": ">=7.52.0",
    "react-native": ">=0.74.0",
    "zod": ">=3.23.0"
  },
  "peerDependenciesMeta": {
    "@hookform/resolvers": { "optional": true },
    "@react-native-async-storage/async-storage": { "optional": true },
    "@tanstack/react-query": { "optional": true },
    "@tanstack/react-router": { "optional": true },
    "react": { "optional": true },
    "react-hook-form": { "optional": true },
    "react-native": { "optional": true },
    "zod": { "optional": true }
  },
  "devDependencies": {
    "@hookform/resolvers": "^3.9.0",
    "@tanstack/react-query": "^5.59.0",
    "@testing-library/react": "^16.1.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "happy-dom": "^15.11.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-hook-form": "^7.52.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "zod": "^3.23.0"
  }
}

 succeeded in 269ms:
{
  "name": "lazuli-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Lazuli runtime spike — Go (under runtime/go and dist/go) + TypeScript (under runtime/ts and dist/ts-<frontend>).",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck"
  },
  "devDependencies": {
    "tsx": "^4.21.0",
    "typescript": "^5.6.0"
  }
}

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
The install updated the lockfile and left the root package manifest unchanged. I’m running the runtime package test suite and typecheck now; I expect any remaining work to be TypeScript/RHF type alignment.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm -C runtime/ts/lazuli test' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm -C runtime/ts/lazuli typecheck' in c:\tmp\migrate-rhf-adapter
2026-05-23T01:34:49.885077Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3.6 seconds
Output:

> @lazuli/runtime@0.0.0 test C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> vitest run


[1m[7m[36m RUN [39m[27m[22m [36mv2.1.9 [39m[90mC:/tmp/migrate-rhf-adapter/runtime/ts/lazuli[39m

 [32m✓[39m src/message-for-error.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 5[2mms[22m[39m
 [32m✓[39m src/route-guard.test.ts [2m([22m[2m5 tests[22m[2m)[22m[90m 4[2mms[22m[39m
 [32m✓[39m src/formatters.test.ts [2m([22m[2m7 tests[22m[2m)[22m[90m 29[2mms[22m[39m
 [32m✓[39m src/client.refresh.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 21[2mms[22m[39m
 [32m✓[39m src/local-setting.web.test.ts [2m([22m[2m3 tests[22m[2m)[22m[90m 17[2mms[22m[39m
 [32m✓[39m src/view-helpers.test.ts [2m([22m[2m12 tests[22m[2m)[22m[90m 23[2mms[22m[39m
 [32m✓[39m src/exports-parity.test.ts [2m([22m[2m2 tests[22m[2m)[22m[90m 3[2mms[22m[39m
 [32m✓[39m src/use-lazuli-route-params.test.tsx [2m([22m[2m3 tests[22m[2m)[22m[90m 22[2mms[22m[39m
 [32m✓[39m tests/tanstack.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 649[2mms[22m[39m
   [33m[2m✓[22m[39m tanstackBeforeLoadGuard[2m > [22mdoes not load the TanStack Router peer from the default React entrypoint [33m614[2mms[22m[39m
 [32m✓[39m src/use-lazuli-action.test.tsx [2m([22m[2m10 tests[22m[2m)[22m[90m 43[2mms[22m[39m
 [32m✓[39m src/use-actor.test.tsx [2m([22m[2m1 test[22m[2m)[22m[90m 99[2mms[22m[39m
 [32m✓[39m src/use-lazuli-form.test.tsx [2m([22m[2m8 tests[22m[2m)[22m[90m 203[2mms[22m[39m
 [32m✓[39m tests/lifecycle-gate.test.tsx [2m([22m[2m7 tests[22m[2m)[22m[90m 266[2mms[22m[39m
 [31m❯[39m src/react-rhf.test.tsx [2m([22m[2m9 tests[22m[2m | [22m[31m6 failed[39m[2m)[22m[90m 199[2mms[22m[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mdoes not overwrite user edits on later hydrate refetches[90m 96[2mms[22m[31m[39m
[31m     → expected 'Ada Lovelace' to be 'Typed Name' // Object.is equality[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22msubmits mapped RHF values through the Lazuli command[90m 2[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mfires onSuccess after a successful mutation[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mmaps validation_failed field errors into RHF formState errors[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'form')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mcalls onError for non-field submit failures[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'form')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mresets to defaults merged with a partial override[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
 [32m✓[39m src/use-lazuli-command.invalidates.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 326[2mms[22m[39m

[2m Test Files [22m [1m[31m1 failed[39m[22m[2m | [22m[1m[32m14 passed[39m[22m[90m (15)[39m
[2m      Tests [22m [1m[31m6 failed[39m[22m[2m | [22m[1m[32m81 passed[39m[22m[90m (87)[39m
[2m   Start at [22m 22:34:47
[2m   Duration [22m 2.36s[2m (transform 2.84s, setup 0ms, collect 10.14s, tests 1.91s, environment 9.19s, prepare 2.86s)[22m

 ELIFECYCLE  Test failed. See above for more details.
[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mthrows a typed error for invalid params without redirect
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.

[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mredirects through the injected router adapter for invalid params
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)
    at LazuliProvider (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react.web.ts:57:27)
    at wrapper (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\use-lazuli-route-params.test.tsx:35:24)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.

[90mstderr[2m | src/react-rhf.test.tsx[2m > [22m[2museLazuliFormRHF[2m > [22m[2mdoes not overwrite user edits on later hydrate refetches
[22m[39mWarning: You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);

[90mstderr[2m | src/react-rhf.test.tsx[2m > [22m[2museLazuliFormRHF[2m > [22m[2mdoes not overwrite user edits on later hydrate refetches
[22m[39mWarning: The current testing environment is not configured to support act(...)

[31m⎯⎯⎯⎯⎯⎯⎯[1m[7m Failed Tests 6 [27m[22m⎯⎯⎯⎯⎯⎯⎯[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mdoes not overwrite user edits on later hydrate refetches
[31m[1mAssertionError[22m: expected 'Ada Lovelace' to be 'Typed Name' // Object.is equality[39m

Expected: [32m"[7mTyped Nam[27me"[39m
Received: [31m"[7mAda Lovelac[27me"[39m

[36m [2m❯[22m src/react-rhf.test.tsx:[2m132:49[22m[39m
    [90m130| [39m
    [90m131| [39m    [35mawait[39m [34mwaitFor[39m(() [33m=>[39m [34mexpect[39m(queryCalls)[33m.[39m[34mtoBeGreaterThanOrEqual[39m([34m2[39m))[33m;[39m
    [90m132| [39m    [34mexpect[39m(result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39mvalue)[33m.[39m[34mtoBe[39m([32m"Typed Name"[39m)[33m;[39m
    [90m   | [39m                                                [31m^[39m
    [90m133| [39m  })[33m;[39m
    [90m134| [39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22msubmits mapped RHF values through the Lazuli command
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m154:30[22m[39m
    [90m152| [39m    )[33m;[39m
    [90m153| [39m
    [90m154| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"  Ada Lovelace  [39m…
    [90m   | [39m                             [31m^[39m
    [90m155| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mage[33m.[39mfield[33m.[39m[34monChange[39m([34m36[39m))[33m;[39m
    [90m156| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m154:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mfires onSuccess after a successful mutation
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m175:30[22m[39m
    [90m173| [39m    [35mconst[39m { result } [33m=[39m [34mrenderHook[39m(() [33m=>[39m [34museProfileForm[39m({ onSuccess })[33m,[39m…
    [90m174| [39m
    [90m175| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"Ada Lovelace"[39m))[33m;[39m
    [90m   | [39m                             [31m^[39m
    [90m176| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m177| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m175:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mmaps validation_failed field errors into RHF formState errors
[31m[1mTypeError[22m: Cannot read properties of null (reading 'form')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m208:28[22m[39m
    [90m206| [39m
    [90m207| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m208| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
    [90m   | [39m                           [31m^[39m
    [90m209| [39m    })[33m;[39m
    [90m210| [39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m207:11[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mcalls onError for non-field submit failures
[31m[1mTypeError[22m: Cannot read properties of null (reading 'form')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m227:28[22m[39m
    [90m225| [39m
    [90m226| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m227| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
    [90m   | [39m                           [31m^[39m
    [90m228| [39m    })[33m;[39m
    [90m229| [39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m226:11[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mresets to defaults merged with a partial override
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m239:30[22m[39m
    [90m237| [39m    [35mconst[39m { result } [33m=[39m [34mrenderHook[39m(() [33m=>[39m [34museProfileForm[39m()[33m,[39m { wrapper })[33m;[39m
    [90m238| [39m
    [90m239| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"Ada Lovelace"[39m))[33m;[39m
    [90m   | [39m                             [31m^[39m
    [90m240| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mage[33m.[39mfield[33m.[39m[34monChange[39m([34m36[39m))[33m;[39m
    [90m241| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34mreset[39m({ age[33m:[39m [34m42[39m }))[33m;[39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m239:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/6]⎯[22m[39m


 exited 1 in 3593ms:

> @lazuli/runtime@0.0.0 test C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> vitest run


[1m[7m[36m RUN [39m[27m[22m [36mv2.1.9 [39m[90mC:/tmp/migrate-rhf-adapter/runtime/ts/lazuli[39m

 [32m✓[39m src/message-for-error.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 5[2mms[22m[39m
 [32m✓[39m src/route-guard.test.ts [2m([22m[2m5 tests[22m[2m)[22m[90m 4[2mms[22m[39m
 [32m✓[39m src/formatters.test.ts [2m([22m[2m7 tests[22m[2m)[22m[90m 29[2mms[22m[39m
 [32m✓[39m src/client.refresh.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 21[2mms[22m[39m
 [32m✓[39m src/local-setting.web.test.ts [2m([22m[2m3 tests[22m[2m)[22m[90m 17[2mms[22m[39m
 [32m✓[39m src/view-helpers.test.ts [2m([22m[2m12 tests[22m[2m)[22m[90m 23[2mms[22m[39m
 [32m✓[39m src/exports-parity.test.ts [2m([22m[2m2 tests[22m[2m)[22m[90m 3[2mms[22m[39m
 [32m✓[39m src/use-lazuli-route-params.test.tsx [2m([22m[2m3 tests[22m[2m)[22m[90m 22[2mms[22m[39m
 [32m✓[39m tests/tanstack.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 649[2mms[22m[39m
   [33m[2m✓[22m[39m tanstackBeforeLoadGuard[2m > [22mdoes not load the TanStack Router peer from the default React entrypoint [33m614[2mms[22m[39m
 [32m✓[39m src/use-lazuli-action.test.tsx [2m([22m[2m10 tests[22m[2m)[22m[90m 43[2mms[22m[39m
 [32m✓[39m src/use-actor.test.tsx [2m([22m[2m1 test[22m[2m)[22m[90m 99[2mms[22m[39m
 [32m✓[39m src/use-lazuli-form.test.tsx [2m([22m[2m8 tests[22m[2m)[22m[90m 203[2mms[22m[39m
 [32m✓[39m tests/lifecycle-gate.test.tsx [2m([22m[2m7 tests[22m[2m)[22m[90m 266[2mms[22m[39m
 [31m❯[39m src/react-rhf.test.tsx [2m([22m[2m9 tests[22m[2m | [22m[31m6 failed[39m[2m)[22m[90m 199[2mms[22m[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mdoes not overwrite user edits on later hydrate refetches[90m 96[2mms[22m[31m[39m
[31m     → expected 'Ada Lovelace' to be 'Typed Name' // Object.is equality[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22msubmits mapped RHF values through the Lazuli command[90m 2[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mfires onSuccess after a successful mutation[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mmaps validation_failed field errors into RHF formState errors[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'form')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mcalls onError for non-field submit failures[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'form')[39m
[31m   [31m×[31m useLazuliFormRHF[2m > [22mresets to defaults merged with a partial override[90m 1[2mms[22m[31m[39m
[31m     → Cannot read properties of null (reading 'fullName')[39m
 [32m✓[39m src/use-lazuli-command.invalidates.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 326[2mms[22m[39m

[2m Test Files [22m [1m[31m1 failed[39m[22m[2m | [22m[1m[32m14 passed[39m[22m[90m (15)[39m
[2m      Tests [22m [1m[31m6 failed[39m[22m[2m | [22m[1m[32m81 passed[39m[22m[90m (87)[39m
[2m   Start at [22m 22:34:47
[2m   Duration [22m 2.36s[2m (transform 2.84s, setup 0ms, collect 10.14s, tests 1.91s, environment 9.19s, prepare 2.86s)[22m

 ELIFECYCLE  Test failed. See above for more details.
[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mthrows a typed error for invalid params without redirect
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.

[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mredirects through the injected router adapter for invalid params
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)
    at LazuliProvider (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react.web.ts:57:27)
    at wrapper (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\use-lazuli-route-params.test.tsx:35:24)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.

[90mstderr[2m | src/react-rhf.test.tsx[2m > [22m[2museLazuliFormRHF[2m > [22m[2mdoes not overwrite user edits on later hydrate refetches
[22m[39mWarning: You called act(async () => ...) without await. This could lead to unexpected testing behaviour, interleaving multiple act calls and mixing their scopes. You should - await act(async () => ...);

[90mstderr[2m | src/react-rhf.test.tsx[2m > [22m[2museLazuliFormRHF[2m > [22m[2mdoes not overwrite user edits on later hydrate refetches
[22m[39mWarning: The current testing environment is not configured to support act(...)

[31m⎯⎯⎯⎯⎯⎯⎯[1m[7m Failed Tests 6 [27m[22m⎯⎯⎯⎯⎯⎯⎯[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mdoes not overwrite user edits on later hydrate refetches
[31m[1mAssertionError[22m: expected 'Ada Lovelace' to be 'Typed Name' // Object.is equality[39m

Expected: [32m"[7mTyped Nam[27me"[39m
Received: [31m"[7mAda Lovelac[27me"[39m

[36m [2m❯[22m src/react-rhf.test.tsx:[2m132:49[22m[39m
    [90m130| [39m
    [90m131| [39m    [35mawait[39m [34mwaitFor[39m(() [33m=>[39m [34mexpect[39m(queryCalls)[33m.[39m[34mtoBeGreaterThanOrEqual[39m([34m2[39m))[33m;[39m
    [90m132| [39m    [34mexpect[39m(result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39mvalue)[33m.[39m[34mtoBe[39m([32m"Typed Name"[39m)[33m;[39m
    [90m   | [39m                                                [31m^[39m
    [90m133| [39m  })[33m;[39m
    [90m134| [39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22msubmits mapped RHF values through the Lazuli command
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m154:30[22m[39m
    [90m152| [39m    )[33m;[39m
    [90m153| [39m
    [90m154| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"  Ada Lovelace  [39m…
    [90m   | [39m                             [31m^[39m
    [90m155| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mage[33m.[39mfield[33m.[39m[34monChange[39m([34m36[39m))[33m;[39m
    [90m156| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m154:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mfires onSuccess after a successful mutation
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m175:30[22m[39m
    [90m173| [39m    [35mconst[39m { result } [33m=[39m [34mrenderHook[39m(() [33m=>[39m [34museProfileForm[39m({ onSuccess })[33m,[39m…
    [90m174| [39m
    [90m175| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"Ada Lovelace"[39m))[33m;[39m
    [90m   | [39m                             [31m^[39m
    [90m176| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m177| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m175:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[3/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mmaps validation_failed field errors into RHF formState errors
[31m[1mTypeError[22m: Cannot read properties of null (reading 'form')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m208:28[22m[39m
    [90m206| [39m
    [90m207| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m208| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
    [90m   | [39m                           [31m^[39m
    [90m209| [39m    })[33m;[39m
    [90m210| [39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m207:11[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[4/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mcalls onError for non-field submit failures
[31m[1mTypeError[22m: Cannot read properties of null (reading 'form')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m227:28[22m[39m
    [90m225| [39m
    [90m226| [39m    [35mawait[39m [34mact[39m([35masync[39m () [33m=>[39m {
    [90m227| [39m      [35mawait[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34monSubmit[39m()[33m;[39m
    [90m   | [39m                           [31m^[39m
    [90m228| [39m    })[33m;[39m
    [90m229| [39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m226:11[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[5/6]⎯[22m[39m

[31m[1m[7m FAIL [27m[22m[39m src/react-rhf.test.tsx[2m > [22museLazuliFormRHF[2m > [22mresets to defaults merged with a partial override
[31m[1mTypeError[22m: Cannot read properties of null (reading 'fullName')[39m
[36m [2m❯[22m src/react-rhf.test.tsx:[2m239:30[22m[39m
    [90m237| [39m    [35mconst[39m { result } [33m=[39m [34mrenderHook[39m(() [33m=>[39m [34museProfileForm[39m()[33m,[39m { wrapper })[33m;[39m
    [90m238| [39m
    [90m239| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mfullName[33m.[39mfield[33m.[39m[34monChange[39m([32m"Ada Lovelace"[39m))[33m;[39m
    [90m   | [39m                             [31m^[39m
    [90m240| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mage[33m.[39mfield[33m.[39m[34monChange[39m([34m36[39m))[33m;[39m
    [90m241| [39m    [34mact[39m(() [33m=>[39m result[33m.[39mcurrent[33m.[39mform[33m.[39m[34mreset[39m({ age[33m:[39m [34m42[39m }))[33m;[39m
[90m [2m❯[22m ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m47:24[22m[39m
[90m [2m❯[22m act ../../../node_modules/.pnpm/react@18.3.1/node_modules/react/cjs/react.development.js:[2m2512:16[22m[39m
[90m [2m❯[22m Proxy.<anonymous> ../../../node_modules/.pnpm/@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d/node_modules/@testing-library/react/dist/act-compat.js:[2m46:25[22m[39m
[90m [2m❯[22m src/react-rhf.test.tsx:[2m239:5[22m[39m

[31m[2m⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[6/6]⎯[22m[39m


 succeeded in 4658ms:

> @lazuli/runtime@0.0.0 typecheck C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> tsc --noEmit


diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..7e18ac808b59b467cbf15b6a7ab862ed13999f93
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,269 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    act(() => result.current.fullName.field.onChange("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    act(() => result.current.fullName.field.onChange("  Ada Lovelace  "));
+    act(() => result.current.age.field.onChange(36));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    act(() => result.current.fullName.field.onChange("Ada Lovelace"));
+    act(() => result.current.age.field.onChange(36));
+    act(() => result.current.form.reset({ age: 42 }));
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
Typecheck is clean. The new tests exposed two test-harness issues: RHF field updates need awaited `act`, and the refetch assertion was checking before the controlled field update had flushed. I’m tightening those tests now rather than changing the adapter behavior.
apply patch
patch: completed
c:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react-rhf.test.tsx
diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm -C runtime/ts/lazuli test -- src/react-rhf.test.tsx' in c:\tmp\migrate-rhf-adapter
 succeeded in 3876ms:

> @lazuli/runtime@0.0.0 test C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> vitest run "src/react-rhf.test.tsx"


[1m[7m[36m RUN [39m[27m[22m [36mv2.1.9 [39m[90mC:/tmp/migrate-rhf-adapter/runtime/ts/lazuli[39m

 [32m✓[39m src/react-rhf.test.tsx [2m([22m[2m9 tests[22m[2m)[22m[90m 243[2mms[22m[39m

[2m Test Files [22m [1m[32m1 passed[39m[22m[90m (1)[39m
[2m      Tests [22m [1m[32m9 passed[39m[22m[90m (9)[39m
[2m   Start at [22m 22:35:14
[2m   Duration [22m 1.29s[2m (transform 103ms, setup 0ms, collect 417ms, tests 243ms, environment 270ms, prepare 107ms)[22m


diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
The focused RHF suite is passing now with 9 coverage cases. I’m re-running the full runtime tests and typecheck to catch integration fallout.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm -C runtime/ts/lazuli test' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm -C runtime/ts/lazuli typecheck' in c:\tmp\migrate-rhf-adapter
 succeeded in 3389ms:

> @lazuli/runtime@0.0.0 test C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> vitest run


[1m[7m[36m RUN [39m[27m[22m [36mv2.1.9 [39m[90mC:/tmp/migrate-rhf-adapter/runtime/ts/lazuli[39m

 [32m✓[39m src/formatters.test.ts [2m([22m[2m7 tests[22m[2m)[22m[90m 31[2mms[22m[39m
 [32m✓[39m src/route-guard.test.ts [2m([22m[2m5 tests[22m[2m)[22m[90m 4[2mms[22m[39m
 [32m✓[39m src/view-helpers.test.ts [2m([22m[2m12 tests[22m[2m)[22m[90m 21[2mms[22m[39m
 [32m✓[39m src/message-for-error.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 5[2mms[22m[39m
 [32m✓[39m src/client.refresh.test.ts [2m([22m[2m6 tests[22m[2m)[22m[90m 15[2mms[22m[39m
 [32m✓[39m src/local-setting.web.test.ts [2m([22m[2m3 tests[22m[2m)[22m[90m 18[2mms[22m[39m
 [32m✓[39m src/exports-parity.test.ts [2m([22m[2m2 tests[22m[2m)[22m[90m 4[2mms[22m[39m
 [32m✓[39m src/use-lazuli-route-params.test.tsx [2m([22m[2m3 tests[22m[2m)[22m[90m 25[2mms[22m[39m
 [32m✓[39m tests/tanstack.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 623[2mms[22m[39m
   [33m[2m✓[22m[39m tanstackBeforeLoadGuard[2m > [22mdoes not load the TanStack Router peer from the default React entrypoint [33m576[2mms[22m[39m
 [32m✓[39m src/use-lazuli-action.test.tsx [2m([22m[2m10 tests[22m[2m)[22m[90m 49[2mms[22m[39m
 [32m✓[39m src/use-actor.test.tsx [2m([22m[2m1 test[22m[2m)[22m[90m 93[2mms[22m[39m
 [32m✓[39m src/use-lazuli-form.test.tsx [2m([22m[2m8 tests[22m[2m)[22m[90m 221[2mms[22m[39m
 [32m✓[39m tests/lifecycle-gate.test.tsx [2m([22m[2m7 tests[22m[2m)[22m[90m 280[2mms[22m[39m
 [32m✓[39m src/react-rhf.test.tsx [2m([22m[2m9 tests[22m[2m)[22m[90m 255[2mms[22m[39m
 [32m✓[39m src/use-lazuli-command.invalidates.test.tsx [2m([22m[2m4 tests[22m[2m)[22m[33m 327[2mms[22m[39m

[2m Test Files [22m [1m[32m15 passed[39m[22m[90m (15)[39m
[2m      Tests [22m [1m[32m87 passed[39m[22m[90m (87)[39m
[2m   Start at [22m 22:35:20
[2m   Duration [22m 2.34s[2m (transform 4.09s, setup 0ms, collect 9.29s, tests 1.97s, environment 10.34s, prepare 2.67s)[22m

[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mthrows a typed error for invalid params without redirect
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.

[90mstderr[2m | src/use-lazuli-route-params.test.tsx[2m > [22m[2museLazuliRouteParams[2m > [22m[2mredirects through the injected router adapter for invalid params
[22m[39mThe above error occurred in the <TestComponent> component:

    at TestComponent (C:\tmp\migrate-rhf-adapter\node_modules\.pnpm\@testing-library+react@16.3_8105afb30b3aed617b9ca863f1e5e96d\node_modules\@testing-library\react\dist\pure.js:328:5)
    at LazuliProvider (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\react.web.ts:57:27)
    at wrapper (C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli\src\use-lazuli-route-params.test.tsx:35:24)

Consider adding an error boundary to your tree to customize error handling behavior.
Visit https://reactjs.org/link/error-boundaries to learn more about error boundaries.


 succeeded in 3475ms:

> @lazuli/runtime@0.0.0 typecheck C:\tmp\migrate-rhf-adapter\runtime\ts\lazuli
> tsc --noEmit


diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff --stat' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
 succeeded in 255ms:
 pnpm-lock.yaml                 | 33 +++++++++++++++++++++++++++++++++
 runtime/ts/lazuli/package.json | 20 +++++++++++++++++---
 2 files changed, 50 insertions(+), 3 deletions(-)

 succeeded in 271ms:
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4..5d3139e 100644
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- pnpm-lock.yaml' in c:\tmp\migrate-rhf-adapter
 succeeded in 255ms:
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 1669e39..4c1cd48 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -49,6 +49,9 @@ importers:
         specifier: ^1.6.0
         version: 1.6.0
     devDependencies:
+      '@hookform/resolvers':
+        specifier: ^3.9.0
+        version: 3.10.0(react-hook-form@7.76.0(react@18.3.1))
       '@tanstack/react-query':
         specifier: ^5.59.0
         version: 5.100.9(react@18.3.1)
@@ -70,12 +73,18 @@ importers:
       react-dom:
         specifier: ^18.3.0
         version: 18.3.1(react@18.3.1)
+      react-hook-form:
+        specifier: ^7.52.0
+        version: 7.76.0(react@18.3.1)
       typescript:
         specifier: ^5.6.0
         version: 5.9.3
       vitest:
         specifier: ^2.1.0
         version: 2.1.9(@types/node@25.9.1)(happy-dom@15.11.7)(terser@5.47.1)
+      zod:
+        specifier: ^3.23.0
+        version: 3.25.76
 
   runtime/ts/playwright:
     devDependencies:
@@ -453,6 +462,11 @@ packages:
     cpu: [x64]
     os: [win32]
 
+  '@hookform/resolvers@3.10.0':
+    resolution: {integrity: sha512-79Dv+3mDF7i+2ajj7SkypSKHhl1cbln1OGavqrsF7p6mbUv11xpqpacPsGDCTRvCSjEEIez2ef1NveSVL3b0Ag==}
+    peerDependencies:
+      react-hook-form: ^7.0.0
+
   '@isaacs/ttlcache@1.4.1':
     resolution: {integrity: sha512-RQgQ4uQ+pLbqXfOmieB91ejmLwvSgv9nLx6sT6sD83s7umBypgg+OIBOBbEUiJXrfpnp9j0mRhYYdzp9uqq3lA==}
     engines: {node: '>=12'}
@@ -1435,6 +1449,12 @@ packages:
     peerDependencies:
       react: ^18.3.1
 
+  react-hook-form@7.76.0:
+    resolution: {integrity: sha512-eKtLGgFeSgkHqQD8J59AMZ9a4uD1D83iSIzt4YlTGD7liDen5rrjcUO1rVIGd9yC1gofryjtHbv+4ny4hkLWlw==}
+    engines: {node: '>=18.0.0'}
+    peerDependencies:
+      react: ^16.8.0 || ^17 || ^18 || ^19
+
   react-is@17.0.2:
     resolution: {integrity: sha512-w2GsyukL62IJnlaff/nRegPQR94C/XXamvMWmSHRJ4y7Ts/4ocGRmTHvOs8PSE6pB3dWOrD/nueuU5sduBsQ4w==}
 
@@ -1789,6 +1809,9 @@ packages:
     resolution: {integrity: sha512-7dSzzRQ++CKnNI/krKnYRV7JKKPUXMEh61soaHKg9mrWEhzFWhFnxPxGl+69cD1Ou63C13NUPCnmIcrvqCuM6w==}
     engines: {node: '>=12'}
 
+  zod@3.25.76:
+    resolution: {integrity: sha512-gzUt/qt81nXsFGKIFcC3YnfEAx5NkunCfnDlvuBSSFS02bcXu4Lmea0AFIUwbLWxWPx3d9p8S5QoaujKcNQxcQ==}
+
 snapshots:
 
   '@babel/code-frame@7.29.0':
@@ -2040,6 +2063,10 @@ snapshots:
   '@esbuild/win32-x64@0.27.7':
     optional: true
 
+  '@hookform/resolvers@3.10.0(react-hook-form@7.76.0(react@18.3.1))':
+    dependencies:
+      react-hook-form: 7.76.0(react@18.3.1)
+
   '@isaacs/ttlcache@1.4.1': {}
 
   '@jest/schemas@29.6.3':
@@ -3080,6 +3107,10 @@ snapshots:
       react: 18.3.1
       scheduler: 0.23.2
 
+  react-hook-form@7.76.0(react@18.3.1):
+    dependencies:
+      react: 18.3.1
+
   react-is@17.0.2: {}
 
   react-is@18.3.1: {}
@@ -3443,3 +3474,5 @@ snapshots:
       string-width: 4.2.3
       y18n: 5.0.8
       yargs-parser: 21.1.1
+
+  zod@3.25.76: {}

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
Full runtime tests and typecheck pass. I’m doing one broader workspace typecheck as a final sanity check because the lockfile changed and this package sits in the pnpm workspace.
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'pnpm typecheck' in c:\tmp\migrate-rhf-adapter
2026-05-23T01:35:38.885025Z ERROR codex_core::tools::router: error=Exit code: 1
Wall time: 3.7 seconds
Output:

> lazuli-monorepo@0.0.0 typecheck C:\tmp\migrate-rhf-adapter
> pnpm -r run typecheck

Scope: 3 of 4 workspace projects
runtime/ts/lazuli typecheck$ tsc --noEmit
runtime/ts/playwright typecheck$ tsc -p tsconfig.json --noEmit
runtime/ts/playwright typecheck: Done
runtime/ts/lazuli typecheck: Done
dist/web/customer typecheck$ tsc --noEmit
dist/web/customer typecheck: src/react.ts(34,12): error TS2314: Generic type 'UseLazuliQueryOptions' requires 1 type argument(s).
dist/web/customer typecheck: src/react.ts(41,12): error TS2314: Generic type 'UseLazuliQueryOptions' requires 1 type argument(s).
dist/web/customer typecheck: Failed
C:\tmp\migrate-rhf-adapter\dist\web\customer:
 ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL  @lazuli/dist-customer@0.0.0 typecheck: `tsc --noEmit`
Exit status 2
 ELIFECYCLE  Command failed with exit code 2.

 exited 1 in 3700ms:

> lazuli-monorepo@0.0.0 typecheck C:\tmp\migrate-rhf-adapter
> pnpm -r run typecheck

Scope: 3 of 4 workspace projects
runtime/ts/lazuli typecheck$ tsc --noEmit
runtime/ts/playwright typecheck$ tsc -p tsconfig.json --noEmit
runtime/ts/playwright typecheck: Done
runtime/ts/lazuli typecheck: Done
dist/web/customer typecheck$ tsc --noEmit
dist/web/customer typecheck: src/react.ts(34,12): error TS2314: Generic type 'UseLazuliQueryOptions' requires 1 type argument(s).
dist/web/customer typecheck: src/react.ts(41,12): error TS2314: Generic type 'UseLazuliQueryOptions' requires 1 type argument(s).
dist/web/customer typecheck: Failed
C:\tmp\migrate-rhf-adapter\dist\web\customer:
 ERR_PNPM_RECURSIVE_RUN_FIRST_FAIL  @lazuli/dist-customer@0.0.0 typecheck: `tsc --noEmit`
Exit status 2
 ELIFECYCLE  Command failed with exit code 2.

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

  ✓ Read required repo guidance and existing runtime patterns
  ✓ Implement RHF adapter and package exports/deps
  ✓ Add tests and STATUS sample
  → Run focused verification and summarize outcome
diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "useLazuliFormRHF|UseLazuliFormRHFOptions|setServerFieldErrors" runtime/ts/lazuli/src/react-rhf.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'rg -n "react/rhf|peerDependencies|@hookform/resolvers|react-hook-form|zod" runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command "rg -n \"describe\\(|HostPersonal\" runtime/ts/lazuli/src/react-rhf.test.tsx STATUS.md" in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short' in c:\tmp\migrate-rhf-adapter
 succeeded in 293ms:
26:export type UseLazuliFormRHFOptions<
54:export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
55:  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
101:        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
123:function setServerFieldErrors<Values extends FieldValues>(

 succeeded in 315ms:
24:    "./react/rhf": {
44:  "peerDependencies": {
45:    "@hookform/resolvers": ">=3.9.0",
50:    "react-hook-form": ">=7.52.0",
52:    "zod": ">=3.23.0"
54:  "peerDependenciesMeta": {
55:    "@hookform/resolvers": { "optional": true },
60:    "react-hook-form": { "optional": true },
62:    "zod": { "optional": true }
65:    "@hookform/resolvers": "^3.9.0",
73:    "react-hook-form": "^7.52.0",
76:    "zod": "^3.23.0"

 succeeded in 288ms:
 M pnpm-lock.yaml
?? CODEX_LOG.txt
?? CODEX_PROMPT.md
?? STATUS.md
?? runtime/ts/lazuli/src/react-rhf.test.tsx

 succeeded in 301ms:
STATUS.md:5:## HostPersonal.tsx Migration Sketch
STATUS.md:10:import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
STATUS.md:13:export function HostPersonal() {
STATUS.md:17:    submit: updateHostPersonal,
runtime/ts/lazuli/src/react-rhf.test.tsx:84:describe("useLazuliFormRHF", () => {

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git ls-files runtime/ts/lazuli/src/react-rhf.ts runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff -- runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'Test-Path runtime/ts/lazuli/src/react-rhf.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git status --short --untracked-files=all' in c:\tmp\migrate-rhf-adapter
 succeeded in 345ms:
runtime/ts/lazuli/package.json
runtime/ts/lazuli/src/react-rhf.ts

 succeeded in 368ms:
 succeeded in 308ms:
 M pnpm-lock.yaml
?? CODEX_LOG.txt
?? CODEX_PROMPT.md
?? STATUS.md
?? runtime/ts/lazuli/src/react-rhf.test.tsx

 succeeded in 320ms:
True

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git diff HEAD -- runtime/ts/lazuli/src/react-rhf.ts' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git ls-files -v runtime/ts/lazuli/src/react-rhf.ts runtime/ts/lazuli/package.json' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git show HEAD:runtime/ts/lazuli/src/react-rhf.ts | Select-Object -First 20' in c:\tmp\migrate-rhf-adapter
exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git show HEAD:runtime/ts/lazuli/package.json | Select-String -Pattern "react/rhf|@hookform|react-hook-form|zod"' in c:\tmp\migrate-rhf-adapter
 succeeded in 859ms:
H runtime/ts/lazuli/package.json
H runtime/ts/lazuli/src/react-rhf.ts

 succeeded in 733ms:
import { zodResolver } from "@hookform/resolvers/zod";
import { useCallback, useEffect, useRef, useState } from "react";
import {
  useForm,
  type DefaultValues,
  type FieldValues,
  type Path,
  type Resolver,
  type UseFormProps,
  type UseFormReturn,
} from "react-hook-form";
import type { ZodSchema } from "zod";

import { isLazuliError } from "./error.js";
import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
import type { CommandSpec, QuerySpec } from "./spec.js";

type EmptyArgs = Record<string, never>;

const emptyArgs: EmptyArgs = {};

 succeeded in 883ms:
 succeeded in 809ms:

    "./[7mreact/rhf[0m": {
    "[7m@hookform[0m/resolvers": ">=3.9.0",
    "[7mreact-hook-form[0m": ">=7.52.0",
    "[7mzod[0m": ">=3.23.0"
    "[7m@hookform[0m/resolvers": { "optional": true },
    "[7mreact-hook-form[0m": { "optional": true },
    "[7mzod[0m": { "optional": true }
    "[7m@hookform[0m/resolvers": "^3.9.0",
    "[7mreact-hook-form[0m": "^7.52.0",
    "[7mzod[0m": "^3.23.0"


diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

exec
"C:\\Program Files\\PowerShell\\7\\pwsh.exe" -Command 'git log -1 --stat --oneline' in c:\tmp\migrate-rhf-adapter
 succeeded in 276ms:
3398271 runtime: @lazuli/runtime/react/rhf — useLazuliFormRHF adapter
 runtime/ts/lazuli/package.json     |  20 ++++-
 runtime/ts/lazuli/src/react-rhf.ts | 171 +++++++++++++++++++++++++++++++++++++
 2 files changed, 188 insertions(+), 3 deletions(-)

diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

  ✓ Read required repo guidance and existing runtime patterns
  ✓ Implement RHF adapter and package exports/deps
  ✓ Add tests and STATUS sample
  ✓ Run focused verification and summarize outcome
diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

codex
Implemented the RHF adapter path and coverage.

What changed:
- Added/verified `useLazuliFormRHF` at `runtime/ts/lazuli/src/react-rhf.ts`.
- Added `./react/rhf` export plus RHF/Zod peer/dev deps in `runtime/ts/lazuli/package.json`.
- Added `runtime/ts/lazuli/src/react-rhf.test.tsx` with 9 tests covering defaults, hydrate-once, submit mapping, success, server field errors, generic errors, reset, and type shape.
- Added `STATUS.md` with a `HostPersonal.tsx` migration sketch.
- Updated `pnpm-lock.yaml`.

Verification:
- `pnpm -C runtime/ts/lazuli test` passed: 87 tests.
- `pnpm -C runtime/ts/lazuli typecheck` passed.
- `pnpm typecheck` at repo root still fails in generated `dist/web/customer/src/react.ts:34` and `:41` because `UseLazuliQueryOptions` is used without its required generic type argument. I did not edit `dist/` per repo rules.
diff --git a/STATUS.md b/STATUS.md
new file mode 100644
index 0000000000000000000000000000000000000000..af1632e572e05a231f1708a3ff4f440dab712899
--- /dev/null
+++ b/STATUS.md
@@ -0,0 +1,56 @@
+# M.2 RHF Adapter Status
+
+Implemented `@lazuli/runtime/react/rhf` with `useLazuliFormRHF`, an RHF-compatible adapter around Lazuli query hydration and command submit.
+
+## HostPersonal.tsx Migration Sketch
+
+```tsx
+import { Controller } from "react-hook-form";
+import { useLazuliFormRHF } from "@lazuli/runtime/react/rhf";
+import { lookupMyHost, updateHostPersonal } from "../../dist/ts-web/host/host.gen";
+import { hostPersonalSchema } from "../../dist/ts-web/host/host.zod";
+
+export function HostPersonal() {
+  const form = useLazuliFormRHF({
+    defaults: { firstName: "", lastName: "", birthDate: null, gender: "" },
+    hydrateFrom: lookupMyHost,
+    submit: updateHostPersonal,
+    schema: hostPersonalSchema.schema,
+    mapValuesToInput: (values) => ({
+      firstName: values.firstName.trim(),
+      lastName: values.lastName.trim(),
+      birthDate: values.birthDate,
+      gender: values.gender || null,
+    }),
+  });
+
+  return (
+    <form onSubmit={(event) => void (event.preventDefault(), form.onSubmit())}>
+      <Controller
+        control={form.control}
+        name="firstName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="lastName"
+        render={({ field, fieldState }) => (
+          <Input value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Controller
+        control={form.control}
+        name="birthDate"
+        render={({ field, fieldState }) => (
+          <DatePicker value={field.value} onChange={field.onChange} error={fieldState.error?.message} />
+        )}
+      />
+      <Button type="submit" disabled={form.isSubmitting}>Save</Button>
+    </form>
+  );
+}
+```
+
+The panel keeps existing `<Controller>` UI bindings while deleting the local `useForm`, `zodResolver`, hydrate effect, save mutation, and field-error mapping boilerplate. The expected shape is about 50 lines for the common settings panels once repeated layout code is left in shared panel primitives.
diff --git a/runtime/ts/lazuli/package.json b/runtime/ts/lazuli/package.json
index 3024ff4d6c266978f42522c50fff166e17439fb6..5d3139e416fa61f4ef82bfc32aacd129e5d87fcf
--- a/runtime/ts/lazuli/package.json
+++ b/runtime/ts/lazuli/package.json
@@ -21,6 +21,11 @@
       "import": "./src/tanstack-adapter.ts",
       "default": "./src/tanstack-adapter.ts"
     },
+    "./react/rhf": {
+      "types": "./src/react-rhf.ts",
+      "import": "./src/react-rhf.ts",
+      "default": "./src/react-rhf.ts"
+    },
     "./formatters": {
       "types": "./src/formatters.ts",
       "import": "./src/formatters.ts",
@@ -37,20 +42,27 @@
     "search-query-parser": "^1.6.0"
   },
   "peerDependencies": {
+    "@hookform/resolvers": ">=3.9.0",
     "@react-native-async-storage/async-storage": ">=1.23.0",
     "@tanstack/react-query": ">=5.0.0",
     "@tanstack/react-router": ">=1.0.0",
     "react": ">=18.0.0",
-    "react-native": ">=0.74.0"
+    "react-hook-form": ">=7.52.0",
+    "react-native": ">=0.74.0",
+    "zod": ">=3.23.0"
   },
   "peerDependenciesMeta": {
+    "@hookform/resolvers": { "optional": true },
     "@react-native-async-storage/async-storage": { "optional": true },
     "@tanstack/react-query": { "optional": true },
     "@tanstack/react-router": { "optional": true },
     "react": { "optional": true },
-    "react-native": { "optional": true }
+    "react-hook-form": { "optional": true },
+    "react-native": { "optional": true },
+    "zod": { "optional": true }
   },
   "devDependencies": {
+    "@hookform/resolvers": "^3.9.0",
     "@tanstack/react-query": "^5.59.0",
     "@testing-library/react": "^16.1.0",
     "@types/react": "^18.3.0",
@@ -58,7 +70,9 @@
     "happy-dom": "^15.11.0",
     "react": "^18.3.0",
     "react-dom": "^18.3.0",
+    "react-hook-form": "^7.52.0",
     "typescript": "^5.6.0",
-    "vitest": "^2.1.0"
+    "vitest": "^2.1.0",
+    "zod": "^3.23.0"
   }
 }
diff --git a/runtime/ts/lazuli/src/react-rhf.test.tsx b/runtime/ts/lazuli/src/react-rhf.test.tsx
new file mode 100644
index 0000000000000000000000000000000000000000..2dadf7d906f3ecfc42ec898bbd10ed5b0c3d8566
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.test.tsx
@@ -0,0 +1,280 @@
+import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
+import { act, renderHook, waitFor } from "@testing-library/react";
+import type { ReactNode } from "react";
+import { useController, type UseFormProps } from "react-hook-form";
+import type { ZodSchema } from "zod";
+import { describe, expect, it, vi } from "vitest";
+
+import { LazuliClient } from "./client.js";
+import { LazuliProvider } from "./react.web.js";
+import { useLazuliFormRHF } from "./react-rhf.js";
+import { defineCommand, defineQuery } from "./spec.js";
+
+type ProfileValues = {
+  fullName: string;
+  email: string;
+  age: number;
+};
+
+const defaults: ProfileValues = { fullName: "", email: "", age: 0 };
+const hydrateProfile = defineQuery<Record<string, never>, Partial<ProfileValues>>("profile.lookup");
+const submitProfile = defineCommand<ProfileValues, { ok: boolean }>("profile.update");
+
+type ProfileFormOptions = {
+  hydrateFrom?: typeof hydrateProfile;
+  mapValuesToInput?: (values: ProfileValues) => ProfileValues;
+  schema?: ZodSchema<ProfileValues>;
+  rhfOptions?: Omit<UseFormProps<ProfileValues>, "defaultValues" | "resolver">;
+  onSuccess?: (out: { ok: boolean }, values: ProfileValues) => void;
+  onError?: (err: unknown, values: ProfileValues) => void;
+};
+
+function makeHarness(fetchImpl?: typeof globalThis.fetch) {
+  const client = new LazuliClient({
+    baseUrl: "http://runtime.test",
+    fetch: fetchImpl ?? okFetch({ ok: true }),
+  });
+  const queryClient = new QueryClient({
+    defaultOptions: { queries: { retry: false }, mutations: { retry: false } },
+  });
+  const wrapper = ({ children }: { children: ReactNode }) => (
+    <QueryClientProvider client={queryClient}>
+      <LazuliProvider client={client}>{children}</LazuliProvider>
+    </QueryClientProvider>
+  );
+  return { queryClient, wrapper };
+}
+
+function useProfileForm(options: ProfileFormOptions = {}) {
+  const form = useLazuliFormRHF({
+    defaults,
+    submit: submitProfile,
+    mapValuesToInput: options.mapValuesToInput ?? ((values) => values),
+    ...(options.hydrateFrom !== undefined ? { hydrateFrom: options.hydrateFrom } : {}),
+    ...(options.schema !== undefined ? { schema: options.schema } : {}),
+    ...(options.rhfOptions !== undefined ? { rhfOptions: options.rhfOptions } : {}),
+    ...(options.onSuccess !== undefined ? { onSuccess: options.onSuccess } : {}),
+    ...(options.onError !== undefined ? { onError: options.onError } : {}),
+  });
+  const fullName = useController<ProfileValues, "fullName">({
+    control: form.control,
+    name: "fullName",
+  });
+  const email = useController<ProfileValues, "email">({
+    control: form.control,
+    name: "email",
+  });
+  const age = useController<ProfileValues, "age">({
+    control: form.control,
+    name: "age",
+  });
+  const errors = form.formState.errors;
+  const isDirty = form.formState.isDirty;
+  return { form, fullName, email, age, errors, isDirty };
+}
+
+function okFetch(body: unknown): typeof globalThis.fetch {
+  return async () => jsonResponse(body);
+}
+
+function jsonResponse(body: unknown, status = 200): Response {
+  return new Response(JSON.stringify(body), { status });
+}
+
+describe("useLazuliFormRHF", () => {
+  it("uses defaults as RHF field values", () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(0);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("hydrates query data into RHF once loaded", async () => {
+    const { wrapper } = makeHarness(async (input) => {
+      expect(String(input)).toContain("/api/v1/q/profile.lookup");
+      return jsonResponse({ full_name: "Ada Lovelace", email: "ada@example.com" });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    expect(result.current.email.field.value).toBe("ada@example.com");
+    expect(result.current.age.field.value).toBe(0);
+  });
+
+  it("does not overwrite user edits on later hydrate refetches", async () => {
+    let queryCalls = 0;
+    const { queryClient, wrapper } = makeHarness(async () => {
+      queryCalls += 1;
+      return jsonResponse({
+        full_name: queryCalls === 1 ? "Ada Lovelace" : "Grace Hopper",
+      });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ hydrateFrom: hydrateProfile }), {
+      wrapper,
+    });
+
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Ada Lovelace"));
+    await act(async () => {
+      result.current.fullName.field.onChange("Typed Name");
+    });
+    await waitFor(() => expect(result.current.fullName.field.value).toBe("Typed Name"));
+
+    await act(async () => {
+      await queryClient.invalidateQueries({ queryKey: ["lazuli", "profile.lookup"] });
+    });
+
+    await waitFor(() => expect(queryCalls).toBeGreaterThanOrEqual(2));
+    expect(result.current.fullName.field.value).toBe("Typed Name");
+  });
+
+  it("submits mapped RHF values through the Lazuli command", async () => {
+    const requests: unknown[] = [];
+    const { wrapper } = makeHarness(async (_input, init) => {
+      requests.push(JSON.parse(String(init?.body)));
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(
+      () =>
+        useProfileForm({
+          mapValuesToInput: (values) => ({
+            fullName: values.fullName.trim(),
+            email: values.email,
+            age: values.age,
+          }),
+        }),
+      { wrapper },
+    );
+
+    await act(async () => {
+      result.current.fullName.field.onChange("  Ada Lovelace  ");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(requests).toEqual([{ full_name: "Ada Lovelace", email: "", age: 36 }]);
+  });
+
+  it("fires onSuccess after a successful mutation", async () => {
+    const events: string[] = [];
+    const onSuccess = vi.fn((out: { ok: boolean }, values: ProfileValues) => {
+      events.push(`success:${String(out.ok)}:${values.fullName}`);
+    });
+    const { wrapper } = makeHarness(async () => {
+      events.push("mutation");
+      return jsonResponse({ ok: true });
+    });
+
+    const { result } = renderHook(() => useProfileForm({ onSuccess }), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+    });
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    expect(events).toEqual(["mutation", "success:true:Ada Lovelace"]);
+    expect(onSuccess).toHaveBeenCalledWith(
+      { ok: true },
+      { fullName: "Ada Lovelace", email: "", age: 0 },
+    );
+  });
+
+  it("maps validation_failed field errors into RHF formState errors", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse(
+        {
+          code: "validation_failed",
+          message: "invalid profile",
+          data: {
+            fields: {
+              full_name: "too_short",
+              email: { message: "invalid_format" },
+            },
+          },
+        },
+        400,
+      ),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.errors.fullName?.message).toBe("too_short"));
+    expect(result.current.errors.fullName?.type).toBe("server");
+    expect(result.current.errors.email?.message).toBe("invalid_format");
+    expect(result.current.form.submitError).toMatchObject({ code: "validation_failed" });
+    expect(onError).not.toHaveBeenCalled();
+  });
+
+  it("calls onError for non-field submit failures", async () => {
+    const onError = vi.fn();
+    const { wrapper } = makeHarness(async () =>
+      jsonResponse({ code: "internal", message: "boom" }, 500),
+    );
+
+    const { result } = renderHook(() => useProfileForm({ onError }), { wrapper });
+
+    await act(async () => {
+      await result.current.form.onSubmit();
+    });
+
+    await waitFor(() => expect(result.current.form.submitError).toMatchObject({ code: "internal" }));
+    expect(onError).toHaveBeenCalledWith(expect.objectContaining({ code: "internal" }), defaults);
+  });
+
+  it("resets to defaults merged with a partial override", async () => {
+    const { wrapper } = makeHarness();
+
+    const { result } = renderHook(() => useProfileForm(), { wrapper });
+
+    await act(async () => {
+      result.current.fullName.field.onChange("Ada Lovelace");
+      result.current.age.field.onChange(36);
+    });
+    await act(async () => {
+      result.current.form.reset({ age: 42 });
+    });
+
+    expect(result.current.fullName.field.value).toBe("");
+    expect(result.current.email.field.value).toBe("");
+    expect(result.current.age.field.value).toBe(42);
+    expect(result.current.isDirty).toBe(false);
+  });
+
+  it("preserves the generic RHF field shape at compile time", () => {
+    function TypeFixture() {
+      const form = useLazuliFormRHF({
+        defaults,
+        submit: submitProfile,
+        mapValuesToInput: (values) => values,
+      });
+
+      form.register("fullName");
+      form.setValue("age", 1);
+      // @ts-expect-error age requires a number.
+      form.setValue("age", "1");
+      // @ts-expect-error field must exist on the form values.
+      form.register("missing");
+
+      return form.watch("age");
+    }
+
+    expect(TypeFixture).toBeTypeOf("function");
+  });
+});
diff --git a/runtime/ts/lazuli/src/react-rhf.ts b/runtime/ts/lazuli/src/react-rhf.ts
new file mode 100644
index 0000000000000000000000000000000000000000..6fed80e2453e9a845e8528f999d85c43487486f2
--- /dev/null
+++ b/runtime/ts/lazuli/src/react-rhf.ts
@@ -0,0 +1,171 @@
+import { zodResolver } from "@hookform/resolvers/zod";
+import { useCallback, useEffect, useRef, useState } from "react";
+import {
+  useForm,
+  type DefaultValues,
+  type FieldValues,
+  type Path,
+  type Resolver,
+  type UseFormProps,
+  type UseFormReturn,
+} from "react-hook-form";
+import type { ZodSchema } from "zod";
+
+import { isLazuliError } from "./error.js";
+import { useLazuliCommand, useLazuliQuery } from "./react.web.js";
+import type { CommandSpec, QuerySpec } from "./spec.js";
+
+type EmptyArgs = Record<string, never>;
+
+const emptyArgs: EmptyArgs = {};
+const disabledHydrateQuery: QuerySpec<EmptyArgs, Partial<object>> = {
+  kind: "query",
+  name: "__lazuli.form_rhf.disabled",
+};
+
+export type UseLazuliFormRHFOptions<
+  Values extends FieldValues,
+  SubmitInput,
+  SubmitOutput,
+> = {
+  defaults: Values;
+  hydrateFrom?: QuerySpec<unknown, Partial<Values>>;
+  submit: CommandSpec<SubmitInput, SubmitOutput>;
+  mapValuesToInput: (values: Values) => SubmitInput;
+  schema?: ZodSchema<Values>;
+  rhfOptions?: Omit<UseFormProps<Values>, "defaultValues" | "resolver">;
+  onSuccess?: (out: SubmitOutput, values: Values) => void;
+  onError?: (err: unknown, values: Values) => void;
+};
+
+export type UseLazuliFormRHFResult<Values extends FieldValues> = Omit<
+  UseFormReturn<Values>,
+  "reset"
+> & {
+  /** Wired submit handler that runs RHF validation, then the Lazuli command. */
+  onSubmit: () => Promise<void>;
+  isSubmitting: boolean;
+  /** Raw error from the last submit, usually LazuliError. */
+  submitError: unknown;
+  /** Reset to defaults, or to defaults merged with a partial override. */
+  reset: (next?: Partial<Values>) => void;
+};
+
+export function useLazuliFormRHF<Values extends FieldValues, Input, Output>(
+  opts: UseLazuliFormRHFOptions<Values, Input, Output>,
+): UseLazuliFormRHFResult<Values> {
+  const hydratedRef = useRef(false);
+  const [submitError, setSubmitError] = useState<unknown>(null);
+
+  const form = useForm<Values>({
+    ...opts.rhfOptions,
+    defaultValues: opts.defaults as DefaultValues<Values>,
+    ...(opts.schema !== undefined
+      ? { resolver: zodResolver(opts.schema) as Resolver<Values> }
+      : {}),
+  });
+  const command = useLazuliCommand(opts.submit);
+  const hydrateSpec = (opts.hydrateFrom ?? disabledHydrateQuery) as QuerySpec<
+    EmptyArgs,
+    Partial<Values>
+  >;
+  const hydrate = useLazuliQuery(hydrateSpec, emptyArgs, {
+    enabled: opts.hydrateFrom !== undefined,
+  });
+
+  const reset = useCallback(
+    (next?: Partial<Values>) => {
+      form.reset(mergeDefaults(opts.defaults, next));
+      form.clearErrors();
+      command.reset();
+      setSubmitError(null);
+    },
+    [command, form, opts.defaults],
+  );
+
+  useEffect(() => {
+    if (hydratedRef.current || hydrate.data === undefined) return;
+    hydratedRef.current = true;
+    form.reset(mergeDefaults(opts.defaults, hydrate.data));
+  }, [form, hydrate.data, opts.defaults]);
+
+  const onSubmit = useCallback(async () => {
+    await form.handleSubmit(async (values) => {
+      form.clearErrors();
+      setSubmitError(null);
+      try {
+        const out = await command.mutateAsync(opts.mapValuesToInput(values));
+        opts.onSuccess?.(out, values);
+      } catch (err) {
+        setSubmitError(err);
+        const usedFieldErrors = setServerFieldErrors(err, values, form.setError);
+        if (!usedFieldErrors) opts.onError?.(err, values);
+      }
+    })();
+  }, [command, form, opts]);
+
+  return {
+    ...form,
+    onSubmit,
+    isSubmitting: form.formState.isSubmitting || command.isPending,
+    submitError,
+    reset,
+  };
+}
+
+function mergeDefaults<Values extends FieldValues>(
+  defaults: Values,
+  next?: Partial<Values>,
+): Values {
+  return { ...defaults, ...(next ?? {}) } as Values;
+}
+
+function setServerFieldErrors<Values extends FieldValues>(
+  err: unknown,
+  values: Values,
+  setError: UseFormReturn<Values>["setError"],
+): boolean {
+  if (!isLazuliError(err) || err.code !== "validation_failed") return false;
+  const fields = asRecord(asRecord(err.data).fields);
+  let used = false;
+  for (const [rawField, rawMessage] of Object.entries(fields)) {
+    const message = fieldMessage(rawMessage);
+    if (!message) continue;
+    setError(normalizeFieldName(rawField, values), { type: "server", message });
+    used = true;
+  }
+  return used;
+}
+
+function fieldMessage(value: unknown): string | null {
+  if (typeof value === "string") return value;
+  if (Array.isArray(value)) {
+    for (const item of value) {
+      const nested = fieldMessage(item);
+      if (nested) return nested;
+    }
+    return null;
+  }
+  const record = asRecord(value);
+  for (const key of ["message", "reason", "code"] as const) {
+    const candidate = record[key];
+    if (typeof candidate === "string") return candidate;
+  }
+  return null;
+}
+
+function normalizeFieldName<Values extends FieldValues>(
+  raw: string,
+  values: Values,
+): Path<Values> {
+  const record = values as Record<PropertyKey, unknown>;
+  if (raw in record) return raw as Path<Values>;
+  const camel = raw.replace(/_([a-z0-9])/g, (_, c: string) => c.toUpperCase());
+  return (camel in record ? camel : raw) as Path<Values>;
+}
+
+function asRecord(value: unknown): Record<string, unknown> {
+  return value !== null && typeof value === "object" && !Array.isArray(value)
+    ? (value as Record<string, unknown>)
+    : {};
+}

ÊXITO: o processo com PID 6852 (processo filho de PID 53584) foi finalizado.
tokens used
132.336
Implemented the RHF adapter path and coverage.

What changed:
- Added/verified `useLazuliFormRHF` at `runtime/ts/lazuli/src/react-rhf.ts`.
- Added `./react/rhf` export plus RHF/Zod peer/dev deps in `runtime/ts/lazuli/package.json`.
- Added `runtime/ts/lazuli/src/react-rhf.test.tsx` with 9 tests covering defaults, hydrate-once, submit mapping, success, server field errors, generic errors, reset, and type shape.
- Added `STATUS.md` with a `HostPersonal.tsx` migration sketch.
- Updated `pnpm-lock.yaml`.

Verification:
- `pnpm -C runtime/ts/lazuli test` passed: 87 tests.
- `pnpm -C runtime/ts/lazuli typecheck` passed.
- `pnpm typecheck` at repo root still fails in generated `dist/web/customer/src/react.ts:34` and `:41` because `UseLazuliQueryOptions` is used without its required generic type argument. I did not edit `dist/` per repo rules.
