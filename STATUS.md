# Cell B.3 Status

Commit hash: `e4be3bb2b5ed43946e0e7697df19fa9c196915c2`

Generator location:

- `crates/lazuli_codegen_ts/src/playwright/fixtures.rs`
- CLI wiring: `crates/lazuli_cli/src/main.rs`

Sample emitted fixture, generated from Hostpoint:

```ts
// Code generated by lazuli; DO NOT EDIT.
import type { APIRequestContext, Page } from '@playwright/test';
import { registerWithRole } from '../../../app/clients/hostpoint-app/e2e/helpers/api';
import type { Role } from '../../../app/clients/hostpoint-app/e2e/helpers/api';
import { signInAs } from '../../../app/clients/hostpoint-app/e2e/helpers/session';
import { progressHostTo, progressTravelerTo } from '../../../app/clients/hostpoint-app/e2e/helpers/onboarding-progress';

export type HostLifecycleState =
  | 'intermediation_terms_pending'
  | 'basic_details_pending'
  | 'address_pending'
  | 'languages_pending'
  | 'complete';
export type TravelerLifecycleState =
  | 'basic_details_pending'
  | 'vehicle_pending'
  | 'family_pending'
  | 'pets_pending'
  | 'languages_pending'
  | 'health_notes_pending'
```

Verification:

- `cargo test -p lazuli_codegen_ts --test playwright_fixtures_golden -- --nocapture`: pass.
- `cargo check -p lazuli_codegen_ts -p lazuli_cli`: pass.
- `cargo test -p lazuli_cli`: pass.
- `.\target\debug\lazuli.exe generate ts examples\full-capsule`: pass; `examples/full-capsule/dist/ts-web/tests/fixtures.gen.ts` exists.
- Hostpoint pilot consumption probe: pass. Generated `c:\Users\lucas\hostpoint\dist\ts-web\tests\fixtures.gen.ts`, copied it into `app/clients/hostpoint-app/e2e/helpers/fixtures.gen.ts` for the co-located helper convention, rewrote only helper import paths to `./...`, and ran `pnpm --filter hostpoint-app test:e2e -- e2e/fixture-probe.generated.spec.ts`; result was 1 passing test.

Notes:

- The generated fixture reuses Hostpoint-owned helpers (`registerWithRole`, `signInAs`, `progressHostTo`, `progressTravelerTo`) and imports only Playwright types.
- Hostpoint's current generated `routesForRole` arrays are empty because the pilot's present `.lzx` route manifest does not yet declare role-reachable route paths; the golden test covers route extraction when route IR supplies those paths.
