app {{app_name}}
  title "{{app_name}}"
  version "0.1.0"

  uses
    account

  environments
    local
    production

  urls
    web local "http://localhost:3000"
    api local "http://localhost:8080"
