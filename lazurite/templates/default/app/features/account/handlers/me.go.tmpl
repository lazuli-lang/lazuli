package handlers

// Me is the @fn.me binding declared in account.lzi.
// Returns the calling actor's User record. The generated code in
// dist/go/ supplies the typed wrapper around this function; you only
// own the body. Replace the stub once your authentication path is wired.
func Me() (any, error) {
    return nil, nil
}
