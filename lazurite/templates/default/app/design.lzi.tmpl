design {{app_slug}}
  # Design tokens live here. Generated frontend adapters consume them
  # from `dist/ts-*`. Each `color`/`space`/`radius`/`shadow`/`motion`/
  # `breakpoint`/`z` token you add becomes available as a TS/CSS variable.
  # See docs/proposals/design-tokens.md for the full token grammar.
  color
    primary "#2563eb"
