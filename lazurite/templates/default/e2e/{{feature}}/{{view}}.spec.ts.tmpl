// Auto-generated by `lazuli generate playwright` (Wave 3.5 — TDD/BDD-first proposal).
//
// File-pair check `TEST-VIEW-E2E-MISSING-001` requires this spec to exist for
// every `view <name>` declared in any `.lzx` under the project. Doctor never
// runs Playwright — it only verifies the file is here. The wire-thin invariant
// stays intact: Lazuli scaffolds, Playwright runs.
//
// Replace every `@TODO authored:` marker with a real assertion. The
// scaffold ships RED on purpose; the test goes GREEN as you finish it.
//
// Severity matrix (per `Lazurite.toml [doctor.profile]`):
//   prototype  → info
//   strict     → warning
//   production → error

import { test, expect } from '@playwright/test';

test.describe('{{feature}}.{{view}}', () => {
  test('renders for authorized audience', async ({ page }) => {
    // @TODO authored: navigate to the view's route
    await page.goto('/{{view}}');
    // @TODO authored: assert visible content from view's anchor/columns
    await expect(page.locator('[data-testid="{{view}}-root"]')).toBeVisible();
  });

  {{#each submits}}
  test('submits {{this}} action', async ({ page }) => {
    // @TODO authored: complete this scenario for command {{this}}
    await page.goto('/{{../view}}');
    // @TODO authored: fill inputs, click submit, assert success path
  });
  {{/each}}
});
